# Current Feature: UI Review Fixes

## Status

In Progress — P1 + P2 implemented and verified; awaiting review + permission to commit.

## Goals

Spec: `context/features/ui-review-fixes-spec.md` (23 findings, tiered P1-P5). Recommended scope for this pass: **P1 + P2**.

**P1 — Verified defects**

- Make homepage body CTAs session-aware — `src/app/page.tsx:27` passes the session only to `SiteNav`, so a signed-in visitor is invited to create an account five times on one page (hero, both pricing CTAs, closing CTA, two footer links, all hardcoded `/register`)

- Remove the dead sidebar "Recent" link (`SidebarNav.tsx:11` → `/dashboard/recent`, no such route) — it lands on Next's unstyled 404 and Next prefetches it on every sidebar render, producing a console 404 per page load. Building the route is a separate backlog item, not this pass

- Add a mobile menu to the homepage nav — `SiteNav.tsx:36` is `hidden … md:flex`, so Features/Pricing vanish below 768px with no hamburger and no substitute

- Fix `ItemCard`'s content preview clipping text mid-glyph (`ItemCard.tsx:64`) — the most-repeated component in the app slices glyphs horizontally instead of ellipsing. Reproduce and confirm the mechanism first; the review's stated cause is probably a mis-measurement of the wrapper

**P2 — Accessibility**

- Name the 17 collapsed sidebar links (`SidebarLink.tsx:31`) — currently anonymous to keyboard and screen-reader users; WCAG 2.4.4 / 4.1.2

- Stop leaking raw type ids into `aria-label`/`title` (`CollectionCard.tsx:53-54` announces `type_prompt`)

- Fix the theme switch's contradictory name/state (`ThemeToggle.tsx:42-44` reads "Switch to light theme, switch, on" while in dark mode)

- Make focus rings consistent — the designed 3px ring is missing on the sidebar logo, Collapse, "All Items", and the homepage logo/Features/Pricing/billing toggle. Fix at the shared level

- Drawer + mobile a11y nits: duplicate "Copy snippet" accessible name, read-only Monaco announced as an editable textbox, mobile nav drawer with no close button or accessible name, homepage footer h2 → h4 skip

**Done this pass (all 12 P1 + P2 items)**

- All twelve implemented, each verified in the browser at the width/theme the review recorded. `npm test` 309 → 328, `npm run build`, `npx eslint src e2e`, `npm run db:test`, and the full 88-spec Playwright suite (fresh dev server, cleared `.next`) all pass. Overflow sweep: 5 pages x 5 widths x signed-out/signed-in = 30 checks, 0 overflow, 0 console errors — the `/dashboard/recent` prefetch 404 is gone

**Deferred (in the spec, not this pass)**

- P3 responsive (6 findings), P4 craft (5 grouped), P5 search — tags are not in the search index (`search.ts:87`)

## Notes

- **Decided 2026-08-20 — pricing CTAs branch on `isPro`.** A signed-in *free* user still sees "Go Pro" (upgrade path preserved); a signed-in *Pro* user gets a dashboard link instead. So `src/app/page.tsx` must pass both `isSignedIn` and `isPro` down, not just `isSignedIn`. Non-pricing CTAs (hero, closing CTA, footer) point at `/dashboard` for any signed-in visitor

- Findings are tagged **[verified]** (confirmed against source during triage, cites file:line) or **[reported]** (browser review only — reproduce before fixing, do not trust the stated cause)

- Source: 2026-08-20 browser-driven UI review at 1440 / 768 / 600 / 390px, both themes, homepage signed-out and signed-in. Read-only against the seeded demo user; no seed data mutated. Screenshots in `.playwright-mcp/` (gitignored)

- Explicitly out of scope: free-tier limits, retuning the fuzzy scorer, `prototypes/homepage/`, rewriting `/favorites` away from its terminal-row style, and adding an `IntersectionObserver` feature check to `Reveal` (known and accepted limitation from Homepage)

- Verification bar from the spec: reproduce each finding at its recorded width/theme before fixing; keep the two historical regression bands clean (hero preview <640, dashboard overflow <563); zero document overflow at all four widths in both themes; the `/dashboard/recent` console 404 count must reach zero; `npm test` / `npm run build` / `npm run lint`, and e2e on a **freshly restarted** dev server since findings 4, 5 and 23 touch markup existing specs locate against

## History

<!-- Keep this updated. Earliest to latest -->

- 2026-04-17: **Dashboard UI Phase 1** — Initialized ShadCN UI, created /dashboard route with TopBar (search + new item button), sidebar and main placeholders, dark mode by default.
- 2026-04-17: **Dashboard UI Phase 2** — Collapsible sidebar (desktop), mobile drawer via Sheet, Favorites/Recent/Types sections wired to mock data, user avatar area (AR initials), hamburger toggle in TopBar. Completed 2026-04-20.
- 2026-04-20: **Dashboard UI Phase 3** — Main content area with StatsGrid (items, collections, favorite items, favorite collections), Recent Collections grid, Pinned Items, and Recent Items. Added reusable Card and Badge UI primitives plus CollectionCard, ItemCard, StatCard, and DashboardSection server components. Completed 2026-04-20.
- 2026-04-20: **Prisma + Neon PostgreSQL Setup** — Installed Prisma 7 with `prisma-client` generator (output `src/generated/prisma`) and the `@prisma/adapter-neon` serverless driver. Added `prisma.config.ts` (Prisma 7 moved datasource URLs out of schema) pointing migrations at the unpooled `DIRECT_URL` while runtime uses pooled `DATABASE_URL` via the adapter. Schema includes app models (User, Item, ItemType, Collection, ItemCollection, Tag) with indexes and cascade deletes, plus NextAuth v5 models (Account, Session, VerificationToken). `src/lib/prisma.ts` exports a singleton client. Initial migration `20260420165113_init` applied to the Neon dev branch. Added idempotent `prisma/seed.ts` (upsert by stable ids) wired via `prisma.config.ts` — seeds the 7 system `ItemType` rows (Snippet, Prompt, Command, Note, Link, File, Image). Completed 2026-04-20.
- 2026-04-20: **Database Seed Script** — Implemented `prisma/seed.ts` per `context/features/seed-spec.md`. Added `bcryptjs` + `@types/bcryptjs`. Seeds demo user (`demo@devstash.io` / password `12345678`, bcrypt 12 rounds, `isPro: false`, `emailVerified` now), aligned 7 system `ItemType` rows with spec colors/Lucide icons, and created 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) with their items linked via `ItemCollection` and tagged with the correct system `ItemType`. Idempotent via upsert on stable ids. Completed 2026-04-20.
- 2026-04-20: **Dashboard Collections (Real Data)** — Replaced mock Recent Collections on the dashboard main area with real data from Neon via Prisma. Added `src/lib/db/collections.ts#getRecentCollections(userId, limit)` (single query with ItemCollection → Item.itemTypeId include; computes item count, unique type ids, and dominant type id). Added `src/lib/type-colors.ts` mapping system type ids to Tailwind border classes. Updated `CollectionCard` to take `CollectionCardData`, render Lucide icons per unique type, and apply dominant-type border color. Dashboard page (`/dashboard`) now server-renders per request (`force-dynamic`) fetching for demo user `user_demo` until auth is wired. StatsGrid, Pinned/Recent Items, and Sidebar remain on mock data (out of scope). Completed 2026-04-20.
- 2026-04-21: **Dashboard Items (Real Data)** — Replaced mock Pinned/Recent Items on the dashboard with real data from Neon via Prisma. Added `src/lib/db/items.ts` with `ItemCardData`, `getPinnedItems(userId)`, `getRecentItems(userId, limit)` (deterministic `updatedAt desc, id desc` ordering), and `getItemsCount(userId)`. Added `getCollectionsCount(userId)` in `src/lib/db/collections.ts`. Reworked `ItemCard` to take `ItemCardData`, derive border color via `getTypeBorderClass` and type-badge icon via `getTypeIcon` (matches `CollectionCard` pattern). `StatsGrid` now takes `itemsCount` + `collectionsCount` props (Favorite Items / Favorite Collections remain on mock). Pinned section is omitted entirely when the user has no pinned items. Dashboard page fetches everything in parallel with `Promise.all`. Extended `scripts/test-db.ts` with expected-vs-actual checks for all 5 collections and all 18 items (id, title, itemTypeId, isPinned/isFavorite flags, ItemCollection link). Completed 2026-04-21.
- 2026-04-21: **Stats & Sidebar (Real Data)** — Wired the last mock-backed dashboard bits to Neon via Prisma. Added `getFavoriteItemsCount`, `getSystemItemTypes` (with `SidebarItemType`) in `src/lib/db/items.ts` and `getFavoriteCollectionsCount`, `getFavoriteCollections` (with `SidebarCollection`) in `src/lib/db/collections.ts`. Added `getTypeDotClass` to `src/lib/type-colors.ts`. `StatsGrid` now takes `favoriteItemsCount` + `favoriteCollectionsCount` (no more `mock-data` imports). Sidebar takes a `SidebarData` prop; `/dashboard` layout fetches `favoriteCollections`, `recentCollections`, and system `itemTypes` in parallel and passes them through `DashboardShell`. Recent section renders a colored dot per `dominantTypeId` (replaces the Folder icon) and keeps the trailing star for favorites; Types section uses real DB rows (capitalized for display) linking to `/items/[typename]`; a "View all collections" link targets `/collections`. Favorites section is omitted when empty. Layout set to `force-dynamic`. Completed 2026-04-21.
- 2026-04-21: **Sidebar Section Collapse** — Per `context/features/sidebar-section-collapse-spec.md`. Added `src/components/dashboard/useCollapsedSections.ts` using `useSyncExternalStore` for SSR-safe `localStorage` sync under key `devstash:sidebar:sections` (shape `{ favorites, recent, types }`, `true` = collapsed). `SidebarSection` header is now a `<button>` with `aria-expanded`/`aria-controls`; a single `ChevronRight` rotates 90° when expanded; panel uses a `grid-template-rows` 0fr↔1fr transition (200ms ease-out) with `inert` on the collapsed panel so keyboard focus skips hidden links. When the outer sidebar is icon-only, per-section toggles are hidden but state still persists. `Sidebar.tsx` wires the hook and passes `sectionId`/`collapsed`/`onToggle`/`sidebarCollapsed` into each section (Favorites, Recent, Types); "View all collections" collapses with Recent. Also added `min-h-0` to the sidebar `ScrollArea` to fix overflow scrolling when content exceeds the viewport. Completed 2026-04-21.
- 2026-04-21: **Sidebar PRO Badge** — Per `context/features/add-pro-badge-sidebar.md`. Added a trailing PRO badge to the `File` and `Image` entries in the sidebar Types section. `Sidebar.tsx` defines `PRO_TYPE_IDS = new Set(["type_file", "type_image"])` and renders a subtle ShadCN `Badge` (variant `outline`, uppercase `PRO`, 10px font with `tracking-wider`, `muted-foreground` text) as the `trailingIcon` on matching `SidebarLink`s. Completed 2026-04-21.
- 2026-04-21: **Codebase Audit Cleanup** — Addressed findings from the 2026-04-21 `nextjs-codebase-auditor` scan (H1, M1, M3–M5, L1–L4). Guarded `DATABASE_URL` with a clear startup error in `src/lib/prisma.ts`; extracted duplicated `DEMO_USER_ID` into `src/lib/constants.ts` and imported from both dashboard layout and page; replaced the full-hydration join in `getRecentCollections` with a `_count` aggregation (kept lightweight `itemTypeId` select for dominant-type computation); added `src/app/dashboard/loading.tsx` skeleton fallback and `src/app/dashboard/error.tsx` client error boundary; moved the inline `capitalize` helper into `src/lib/format.ts`; removed redundant `"use client"` from `SidebarNav.tsx` (SidebarLink already declares its own client boundary); extracted `TypeDot` into `src/components/dashboard/TypeDot.tsx`; made the demo seed password overridable via `SEED_DEMO_PASSWORD` env var with `"12345678"` fallback; and moved `SYSTEM_ITEM_TYPES`/`COLLECTIONS`/`ITEMS` into shared `prisma/seed-data.ts` imported by both `seed.ts` and `scripts/test-db.ts` (fixes pre-existing test-db drift where expected icon/name values no longer matched the seed). M2 (dead `mock-data.ts` helpers) deferred. Completed 2026-04-21.
- 2026-04-21: **Auth Phase 1 — NextAuth v5 + GitHub OAuth** — Per `context/features/auth-spec-files/auth-phase-1-spec.md`. Installed `next-auth@beta` (v5.0.0-beta.31) and `@auth/prisma-adapter`. Split config for edge safety: `src/auth.config.ts` exports `{ providers: [GitHub] } satisfies NextAuthConfig`; `src/auth.ts` wires `PrismaAdapter(prisma)` + `session: { strategy: "jwt" }` and a session callback that copies `token.sub` → `session.user.id`. Added `src/app/api/auth/[...nextauth]/route.ts` re-exporting `GET/POST` from `handlers`, and `src/types/next-auth.d.ts` extending `Session.user` with `id: string`. Route protection via `src/proxy.ts` (named `proxy = auth(...)`, `matcher: ["/dashboard/:path*"]`) redirects unauthenticated traffic to `/api/auth/signin?callbackUrl=...` (NextAuth default page, no custom `pages.signIn`). Documented `AUTH_SECRET`, `AUTH_GITHUB_ID`, `AUTH_GITHUB_SECRET` in `.env.example`; `AUTH_TRUST_HOST=true` set in local `.env` for `next start` (dev auto-trusts localhost). Completed 2026-04-21.
- 2026-04-21: **Auth Phase 2 — Credentials Provider (Email/Password)** — Per `context/features/auth-spec-files/auth-phase-2-spec.md`. Added `Credentials` provider with `authorize: () => null` placeholder in `src/auth.config.ts`; overrode it in `src/auth.ts` (Node runtime) with real bcryptjs validation (zod-parses email + password, looks up user via Prisma, `bcrypt.compare`). Created `POST /api/auth/register` at `src/app/api/auth/register/route.ts` — Zod schema validates `{ name, email, password ≥8, confirmPassword }` with match refinement; returns 201 with `{ success, user }`, 400 for invalid input, 409 for duplicate email; bcrypt hashes at 12 rounds. Installed `zod ^4.3.6` as direct dep (first use of Zod in the project per coding standards). `User.password` field already present from Phase 1 seed setup — no migration needed. GitHub OAuth from Phase 1 preserved unchanged. Completed 2026-04-21.
- 2026-04-21: **Auth Phase 3 — Custom Auth Pages & Real Sidebar User** — Per `context/features/auth-spec-files/auth-phase-3-spec.md`. Created `(auth)` route group with centered layout; server-component pages for `/sign-in` and `/register` with extracted client form components (`SignInForm`, `RegisterForm`) for the `"use client"` boundary. `SignInForm` handles GitHub OAuth + credentials sign-in with loading states; `RegisterForm` POSTs to `/api/auth/register` and redirects to `/sign-in?registered=1`. Added `useEffect` in `SignInForm` to detect `?registered=1` param and fire a sonner toast — avoids cross-navigation toast loss. Installed `sonner ^2.0.7`; added `<Toaster>` to root layout. Updated `src/auth.config.ts` with `pages: { signIn: "/sign-in" }`. Created `UserAvatar` component (GitHub image or initials fallback). Wired `auth()` session data from dashboard layout through `DashboardShell` → `Sidebar` → `UserMenu` props; `UserMenu` shows real name/avatar with click-toggle dropdown (Profile link + Sign out). Completed 2026-04-21.
- 2026-04-21: **Email Verification on Register** — Installed `resend`. After credentials registration, `POST /api/auth/register` generates a `randomBytes(32)` token, stores it in `VerificationToken` (24h expiry), and sends a verification email via Resend. `GET /verify-email` (under `(auth)` route group) validates the token, stamps `User.emailVerified`, deletes the token, and redirects to `/sign-in?verified=1` (or `?verify_error=expired/invalid`). `src/auth.ts` throws `EmailNotVerifiedError extends CredentialsSignin` (code `"email_not_verified"`) instead of returning null for unverified users. `RegisterForm` shows a "check your inbox" state on success. `SignInForm` handles `?verified=1` success toast and the `email_not_verified` error code with a targeted message. Added `scripts/purge-users.ts` (`npm run db:purge-users`) to delete all non-demo users and their content. Completed 2026-04-21.
- 2026-04-30: **Email Verification Toggle Flag** — Added `EMAIL_VERIFICATION_ENABLED` env var (default off). When unset or `"false"`: registration stamps `emailVerified: new Date()` immediately and skips the Resend token+send; `src/auth.ts` bypasses the `EmailNotVerifiedError` check so unverified users can sign in. When `"true"`: full verification flow preserved unchanged. Documented in `.env.example`. Completed 2026-04-30.
- 2026-04-30: **Forgot Password** — Added full password reset flow. "Forgot password?" link added to `SignInForm` (next to PASSWORD label). `/forgot-password` page with `ForgotPasswordForm` calls `forgotPassword` Server Action: validates email exists (explicit error if not), creates a `VerificationToken` with `reset:email` prefix identifier (24h TTL), sends reset email via Resend. `/reset-password?token=...` server page pre-validates token and renders `ResetPasswordForm` or an invalid-link error card. `resetPassword` Server Action re-validates token, hashes new password with bcrypt, updates user, deletes token (single-use). Redirects to `/sign-in?password_reset=1` on success; `SignInForm` fires Sonner toast. Spec at `docs/superpowers/specs/2026-04-30-forgot-password-design.md`. Completed 2026-04-30.
- 2026-05-26: **Auth Routes Refactor & Sign-In Banner** — Moved forgot/reset password from Server Actions to API route handlers under `src/app/api/auth/{forgot,reset}-password/route.ts` to match the existing `/api/auth/register` pattern; deleted `src/actions/auth.ts`. `ForgotPasswordForm` and `ResetPasswordForm` now `fetch()` with `{ success, error? }` JSON envelopes and semantic status codes (200/400/404/410/502). Fixed a Sonner race condition where `?password_reset=1`, `?verified=1`, and `?registered=1` toasts on `/sign-in` were silently dropped — root cause: Sonner's `<Toaster>` subscribes inside its own `useEffect`, and React runs child effects before parent effects on mount, so `SignInForm` published to zero subscribers and Sonner does not replay queued toasts on subscription (confirmed via `toast.getHistory()` showing the queued-but-never-displayed entry). Replaced the toast logic with a server-rendered `role="status"` banner in `sign-in/page.tsx` (reads `searchParams` server-side, covers all five status flags including `verify_error=expired|invalid`). Removed the now-dead `<Toaster>` mount and uninstalled `sonner`. Code-quality cleanup: extracted the duplicated `"reset:"` token-identifier prefix into `PASSWORD_RESET_TOKEN_PREFIX` in `src/lib/constants.ts` (consumed by both routes and the reset-password page), replaced deprecated `React.FormEvent` in both forms with the structural `{ preventDefault(): void }` type already used in `SignInForm`. Completed 2026-05-26.
- 2026-05-26: **Profile Page** — New auth-gated `/profile` route. Updated `src/proxy.ts` matcher to include `/profile/:path*` with a shared `PROTECTED_PREFIXES` array; page-level `redirect("/sign-in?callbackUrl=/profile")` adds defense-in-depth. `src/app/profile/page.tsx` is a server component that fetches session via `auth()` and runs user lookup + counts in parallel with `Promise.all`. Renders `ProfileUserInfo` (name, email, member-since date, avatar via the existing `UserAvatar` for GitHub-image-or-initials) and `ProfileStats` (Items + Collections stat cards reusing `StatCard`; "By type" list showing all 7 system types using `getTypeIcon` + `getTypeDotClass`). Added `getItemsCountByType(userId)` to `src/lib/db/items.ts` using Prisma `_count` with a `where: { userId }` filter so all type rows appear even at zero. `ChangePasswordForm` renders only when `User.password` is not null (skips GitHub-OAuth-only accounts); `POST /api/auth/change-password` validates session, parses with Zod, runs `bcrypt.compare` for current password, writes `bcrypt(12)` hash for new. `DeleteAccountDialog` uses a new `src/components/ui/alert-dialog.tsx` ShadCN-style wrapper over `@base-ui/react/alert-dialog` (matches the existing `tooltip.tsx` pattern); the confirm button stays disabled until the user types their exact email (case-insensitive). `DELETE /api/auth/delete-account` removes any `VerificationToken` rows for the email (both `identifier = email` and `reset:email`, since tokens aren't FK-linked), then deletes the User (Prisma `onDelete: Cascade` handles Items, Collections, Sessions, Accounts, ItemTypes). Client `signOut({ callbackUrl: "/sign-in" })` clears the cookie after success. Sidebar `UserMenu` Profile link (already configured in auth phase 3) now lands on a real page. Spec at `context/features/profile-spec.md`. Completed 2026-05-26.
- 2026-05-28: **Rate Limiting for Auth** — Per `context/features/rate-limiting-spec.md`. Installed `@upstash/redis` + `@upstash/ratelimit`. Built `src/lib/rate-limit.ts` with a lazy Upstash client (three-state cache: `undefined`/`null`/`Redis`), per-key cached `Ratelimit.slidingWindow` instances, `getClientIp(request)` honoring `x-forwarded-for` (first hop) → `x-real-ip` → `"unknown"`, a typed `rateLimit(key, identifier)` returning `{ success } | { success, retryAfterSeconds }` that **fails open** on missing env or transport errors, `rateLimitMessage(seconds)` for consistent copy, and `rateLimitResponse(seconds)` returning a 429 `NextResponse` with `Retry-After` header. Applied limits: `/api/auth/register` 3/1h IP, `/api/auth/forgot-password` 3/1h IP, `/api/auth/reset-password` 5/15m IP, `/api/auth/resend-verification` (new endpoint, gated by `EMAIL_VERIFICATION_ENABLED`) 3/15m IP+email, and credentials login 5/15m IP+email enforced inside `authorize()` (NextAuth v5 hands the Request as the 2nd arg). Login surfaces the limit as `RateLimitError extends CredentialsSignin` with `code: "rate_limited"` — `SignInForm` maps that code to a friendly inline message; the other four routes return JSON `{ error }` with semantic copy and a `Retry-After` header. Side effect in `authorize()`: emails are now `.toLowerCase()`'d before lookup (needed for limit-key stability with the lowercased canonical storage form; also fixes a latent case-sensitivity sign-in bug). `RegisterForm` gained a "Resend verification email" button on the inbox screen and the register route now returns `verificationEmailSent: boolean` so the form redirects to `/sign-in?registered=1` when `EMAIL_VERIFICATION_ENABLED=false` instead of showing the misleading "Check your inbox" screen. Documented `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` in `.env.example`. Completed 2026-05-28.
- 2026-06-22: **Fix GitHub OAuth Redirect** — Per `context/fixes/github-oatuh-redirect-fix.md`. Fixed the two-click GitHub sign-in bug (first click authenticated but bounced back to `/sign-in` instead of redirecting to `/dashboard`) by switching from client-side `signIn` (`next-auth/react`) to the NextAuth v5 server-side pattern. Re-created `src/actions/auth.ts` (deleted in the 2026-05-26 refactor) with a `"use server"` `signInWithGitHub(formData)` action calling `signIn("github", { redirectTo })` from `@/auth`. In `SignInForm.tsx`, replaced the GitHub `<Button onClick={handleGitHub}>` with a `<form action={signInWithGitHub}>` carrying a hidden `callbackUrl` input (preserves the existing `?callbackUrl=` param behavior, defaulting to `/dashboard`); removed `githubLoading` state and `handleGitHub`. Credentials login unchanged (still `signIn("credentials", { redirect: false })`). `npm run build` passes; the pre-existing unrelated `ItemCard.tsx` lint error remains. Completed 2026-06-22.
- 2026-06-22: **Items List View** — Per `context/features/item-list-view-spec.md`. Added dynamic route `/items/[type]` (`src/app/items/[type]/page.tsx`, server component, `force-dynamic`) that resolves the URL slug against the system item types via `getTypeSlug(name)` (lowercase + `"s"`, matching the existing sidebar `href`) and calls `notFound()` for unknown slugs. Added `getItemsByType(userId, itemTypeId)` to `src/lib/db/items.ts` (reuses `ITEM_SELECT`/`toCardData` and the standard `updatedAt desc, id desc` ordering). Items render in a responsive `ItemCard` grid (`grid-cols-1 md:grid-cols-2`) with a per-type count heading and an empty state. Added `src/app/items/layout.tsx` mirroring the dashboard layout so the page renders inside `DashboardShell` (sidebar Types links now resolve to a real, navigable page instead of a 404). Also restyled `ItemCard` and `CollectionCard` from a full `border-2` to a left-only `border-l-4` accent via new `getTypeLeftBorderClass` (replacing the now-removed `getTypeBorderClass`/`TYPE_BORDER_CLASS`), and switched the command type icon from `Command` (⌘) to `Terminal` (bash-style `>_`). Scoped to the demo user (`DEMO_USER_ID`) until per-page auth is wired. `npm run build` passes; verified end-to-end with the dev server (`/items/snippets` → 200 with items, `/items/links` → links, `/items/bogus` → 404). Completed 2026-06-22.
- 2026-06-25: **Quick Copy on Items** — Added a one-click copy action to every item card. New `"use client"` `CopyButton` (`src/components/dashboard/CopyButton.tsx`) calls `navigator.clipboard.writeText`, swaps the Lucide `Copy` icon to a green `Check` for 2s on success (auto-reset via `useEffect` timeout), and exposes accessible `aria-label`/`title` that flip to "Copied". `ItemCard` (kept a server component) renders it in the card header next to the favorite `Star`, passing `item.content ?? item.url` as the value with a type-aware label (`Copy <type>`) and showing nothing when both are empty — so snippets/commands/notes copy `content` and links copy `url`. Uses the existing `Button` `size="icon-xs"` ghost variant and `size-3.5` icon sizing for consistency. Applies everywhere `ItemCard` renders (`/items/[type]` grid + dashboard Pinned/Recent). `npm run build` passes. Completed 2026-06-25.
- 2026-06-25: **Vitest Unit Testing Setup** — Per `docs/superpowers/specs/2026-06-25-vitest-setup-design.md`. Introduced Vitest for unit testing, scoped to server-side utilities and `lib/db` helpers (not React components). Installed `vitest` + `@vitest/coverage-v8`; `vitest.config.ts` at root uses `environment: "node"`, `include: ["src/**/*.test.ts"]`, native `resolve.tsconfigPaths` (Vitest 4 resolves the `@/*` alias from `tsconfig.json` without a plugin — `vite-tsconfig-paths` was installed then dropped on the runtime's own recommendation), and v8 coverage over `src/lib/**` (excluding `prisma.ts`, `mock-data.ts`, `type-icons.tsx`). Added `test` (`vitest run`), `test:watch`, and `test:coverage` scripts. Wrote 5 colocated test files (27 tests) with explicit Vitest imports (no globals): `format`, `type-colors`, `rate-limit` (pure utilities), and `db/collections` + `db/items` (data-shaping with the Prisma singleton mocked via `vi.mock("@/lib/prisma")`). Documented the workflow change in `context/ai-interaction.md` (rewrote step 4 to require `npm test` + tests for touched utility/`lib/db`/action logic, added a Testing section), `context/coding-standards.md` (Testing section), and `CLAUDE.md` (Commands). `npm test` 27/27, `npm run test:coverage`, and `npm run build` all pass; the pre-existing unrelated `ItemCard.tsx` lint error remains. Completed 2026-06-25.
- 2026-06-25: **Item List 3-Column Grid** — Stepped the items-by-type list view (`src/app/items/[type]/page.tsx`) grid up from a 2-column max to a 3-column max on large viewports by changing the `ItemCard` grid wrapper from `grid-cols-1 md:grid-cols-2` to `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`. Stays responsive: 1 column on mobile, 2 at `md`, 3 at `lg`+. Scope limited to this list view — dashboard Pinned/Recent grids use their own `DashboardSection` wrappers and were untouched. No server-action or utility logic changed, so no new unit tests were warranted; existing suite stays green (27/27). Completed 2026-06-25.
- 2026-07-01: **Theme Switch** — Added a dashboard top-bar theme switch with Sun/Moon icons, persisted the selected light/dark theme in `localStorage` under `devstash:theme`, and applied the stored theme before hydration from the root layout to avoid a theme flash. Verified with `npm test`, `npm run build`, and browser flow sign-in/toggle/reload checks. Completed 2026-07-01.
- 2026-08-04: **Item Drawer** — Per `context/features/item-drawer-spec.md`. Added a right-side `Sheet` drawer as the item detail view (no separate item page). New `GET /api/items/[id]` (`src/app/api/items/[id]/route.ts`) is session-gated via `auth()` (401 without a session) and ownership-scoped (404 when the item is missing or belongs to another user); it delegates to the new `getItemDetail(userId, itemId)` in `src/lib/db/items.ts`, which adds `ItemDetailData` (extends `ItemCardData` with `language`, `isPinned`, `collections`, and ISO-string `createdAt`/`updatedAt` — serialized server-side so the client never receives a `Date`). `ItemDrawerProvider` (mounted once in `DashboardShell`, so it covers both `/dashboard` and `/items/[type]`) owns open state, exposes `openItem(id)` through context, caches fetched details in a `Map` for the session, and guards against stale responses by comparing the resolved id against a `currentId` ref. `ItemCardTrigger` is a `role="button"` div wrapper (not a `<button>`, since the card already contains the Copy button) handling click plus Enter/Space; `ItemCard` stays a server component and gained `h-full` (needed now that the wrapper is the grid child) plus a hover tint. `CopyButton` now calls `stopPropagation()` so copying doesn't also open the drawer. Drawer layout follows `context/screenshots/dashboard-ui-drawer.png`: type-colored icon tile, title with `Type · language` subtitle, a single action bar (Favorite / Pin / Copy / Edit / Delete with Delete right-aligned), description, CONTENT block, URL block for links, TAGS, COLLECTIONS, and a Created/Updated footer. Per decision, the action bar is **display only** this pass — Copy is live, Favorite/Pin show state but are disabled, Edit/Delete are disabled placeholders; no mutation endpoints were added. Also added `formatIsoDate` to `src/lib/format.ts`, `getTypeTextClass` to `src/lib/type-colors.ts`, and (incidentally) extracted `TypeGlyph` into `src/lib/type-icons.tsx`, adopting it in `ItemCard` to clear a pre-existing `react-hooks/static-components` lint error — lint is now 0 errors. Tests went 36 → 41: `src/app/api/items/[id]/route.test.ts` is the first API route-handler test in the suite (mocks `@/auth` and `@/lib/db/items`, covering 200 / scoping / 401 / 401-without-id / 404), plus `getItemDetail`, `formatIsoDate`, and `getTypeTextClass` unit tests; all five guards were mutation-checked. Verified in the browser as the demo user across dark and light themes: click and keyboard open, snippet/link/command/note types, skeleton under a delayed response, error card on a 500, Copy without opening the drawer, cached reopen firing no request, and a slow response for one item not overwriting another. Known limitation: the pages still fetch with `DEMO_USER_ID` while the API scopes to `session.user.id` — they agree only for the seeded demo login (`user_demo`), so a GitHub-OAuth session would 404 on open until the pages move to session-scoped fetching. Completed 2026-08-04.
- 2026-08-04: **Item Drawer — Edit Mode** — Per `context/features/item-drawer-edit-spec.md`. The drawer's pencil button now toggles inline edit mode instead of being a disabled placeholder: the same `Sheet` stays open, the header and Created/Updated footer persist, the action bar is replaced by Save/Cancel, and the body becomes controlled inputs (no form library). New `updateItem` server action in `src/actions/items.ts` — Zod-validated, session via `auth()`, returns the `{ success, data, error }` envelope; paired with `updateItem(userId, itemId, data)` in `src/lib/db/items.ts`, which verifies ownership with `findFirst({ id, userId })` before updating (so no coupling to Prisma's `P2025`), replaces tags via `set: []` + `connectOrCreate`, and returns the refreshed `ItemDetailData` so the drawer updates without a second fetch. Extracted `src/lib/item-form.ts` (`getEditableFields`, `buildUpdatePayload`) so the type gating is unit-testable: content for snippet/prompt/command/note, language for snippet/command, url for link; fields the form does not render are carried over from the stored item so a save never clears them. Two validation subtleties: `content` uses a non-trimming schema because indentation and trailing newlines are significant in code (a review caught the original shared schema silently stripping them — pinned with a failing test first), while title/description/language/url stay trimmed; and the form is `noValidate` so the server Zod schema is the single source of truth and its errors arrive as toasts rather than native `type="url"` bubbles blocking submit. `ItemDrawerProvider` gained `editing` state (reset on open and on close), and `handleSaved` replaces the cache entry as well as the state before calling `router.refresh()` so the server-rendered card lists follow (~1.3s). Re-added `sonner` (uninstalled 2026-05-26) for save toasts, mounted via a new `src/components/theme/ThemeToaster.tsx` that follows the app's own `dark` class and `devstash:theme-change` event instead of the OS preference — chosen over the earlier server-rendered-banner approach because the drawer stays mounted, so the 2026-05-26 subscription race does not apply. Added a `Textarea` UI primitive (base-ui has none). Tests 58 → 71 across `src/actions/items.test.ts`, `src/lib/item-form.test.ts`, and `src/lib/db/items.test.ts`; the new assertions were mutation-checked, and the tag `set` + `connectOrCreate` ordering was verified against the dev database with a throwaway probe (leaving a few orphaned `Tag` rows, which `connectOrCreate` reuses). Verified in the browser across snippet/prompt/command/link in both themes: field gating, Cancel discarding, Save-disabled-on-empty-title, invalid-URL toast, lossless content round-trip, and `npm run db:test` green afterwards. Known limitations left unfixed: pages still fetch with `DEMO_USER_ID` while the action scopes to `session.user.id` (so editing only works as the seeded demo user); `context/coding-standards.md` still calls route-handler tests "(later)"; `vitest.config.ts` scopes coverage to `src/lib/**`, excluding the action and API route; `z.string().url()` is the older Zod 4 spelling; `url` stays optional for links per spec; no seeded `note` items exist to exercise that type. Completed 2026-08-04.
- 2026-08-04: **Item Delete** — The drawer's trash button was a disabled placeholder; it now opens a confirmation and deletes. New `DeleteItemDialog` (client) wraps the existing `src/components/ui/alert-dialog.tsx` (hand-rolled over `@base-ui/react`, same `render`-prop composition as `DeleteAccountDialog`), names the item in its description, and ignores dismiss attempts while the request is in flight so the dialog cannot vanish mid-delete. Confirm button is labelled "Delete" rather than "Delete item" — that is the trigger's label, and duplicating it announced the same accessible name twice. Dialog sits at `z-60` over the Sheet's `z-50` (documented; portal DOM order alone would be fragile). Server action `deleteItem(itemId)` in `src/actions/items.ts` mirrors `updateItem` (`auth()` gate, `{ success, error }` envelope) and rejects a blank id with a plain `!itemId?.trim()` check — a Zod schema here would coerce, transform and report nothing. `deleteItem(userId, itemId)` in `src/lib/db/items.ts` verifies ownership with `findFirst({ id, userId })` before deleting, so a missing or foreign item returns `false` instead of coupling to Prisma's `P2025`; `ItemCollection` links and implicit tag links cascade with the row while shared `Tag` rows survive (verified against the dev DB with a throwaway probe). `ItemDrawerProvider` gained `handleDeleted`, which **evicts** the cached detail (`cache.current.delete(id)`) rather than replacing it as save does — a kept entry would let `openItem` replay a ghost with no refetch — then closes the drawer and `router.refresh()`es the server-rendered lists. Also reworked the drawer's error state into `ItemDrawerErrorState { title, message }`: a 404 now reads "Item unavailable — This item no longer exists — it may have been deleted." instead of "Something went wrong", a state reachable in the window between a delete and the list re-rendering. The 404 is checked before `response.json()` (that path never reads the body, and a 404 from elsewhere may not carry JSON), and the stale-response guard now runs after both awaits. Tests 71 → 80 in `src/actions/items.test.ts` and `src/lib/db/items.test.ts`; every new guard was mutation-checked. **First Playwright E2E layer in the project** (`npm run test:e2e`, 10 specs): `playwright.config.ts` (chromium, `workers: 1`, `webServer` reusing/starting `npm run dev`), `e2e/auth.setup.ts` which caches `storageState` and reuses it rather than re-signing-in — credentials login is rate limited 5/15m, so a fresh login each run would lock the suite out — and `scripts/e2e-item-fixture.ts`, a `tsx` subprocess CLI for DB fixtures because Playwright transpiles specs to CJS while the generated Prisma client is ESM. Every spec works on a throwaway item, so seeded data stays intact. The cache-eviction spec recreates the deleted id with a new title and navigates client-side (same layout, provider stays mounted) to prove a refetch; it was initially vacuous — an earlier attempt aborted the RSC refresh, which made Next hard-navigate, and a stale dev server reused via `reuseExistingServer` served pre-mutation code during the first mutation check. Known limitations: pages still fetch with `DEMO_USER_ID` while the action scopes to `session.user.id`, so delete works only as the seeded demo user; delete is drawer-only (no per-card action); an item deleted **out of band** (another tab/device) can still show a cached ghost, since eviction only fires for deletes made through this drawer; and the testing docs (`coding-standards.md`, `ai-interaction.md`, `CLAUDE.md`) still describe Vitest-only testing with no mention of `npm run test:e2e`. Completed 2026-08-04.
- 2026-08-04: **Item Create** — Per `context/features/item-create-spec.md`. The TopBar "New Item" button was inert; it now opens a modal that picks a type and renders only the fields that type uses. Added the project's first `Dialog` primitive at `src/components/ui/dialog.tsx` over `@base-ui/react/dialog` (only `alert-dialog.tsx` existed) following the same hand-rolled wrapper pattern — Portal + Backdrop + Popup, Title/Description/Close, and an opt-in `closeDisabled` so a dialog that blocks dismissal mid-request doesn't leave an inert X beside a disabled Cancel. `NewItemDialog` (client) lives in the TopBar and receives `itemTypes` from `sidebarData` via `DashboardShell`, so no new fetch was added. The type selector is a native radio group styled as chips (single tab stop, arrow keys move selection, visible focus ring — verified in the browser). `createItem` server action in `src/actions/items.ts`: Zod, `auth()` gate, `{ success, data, error }`; extracted `sharedItemFields`/`tagList` so the create and update schemas can't drift. `createItem(userId, data)` in `src/lib/db/items.ts` sets `contentType: "text"` explicitly — required by the schema with no default — and connect-or-creates tags. Three decisions beyond the literal spec: field visibility comes from the existing `getEditableFields` rather than a second copy of the matrix, and the action **re-applies** it server-side so a hand-rolled request sending `content` for a link (or a `url` for a note) cannot store an unreachable field; `orderCreatableTypes` was added because `getSystemItemTypes()` sorts alphabetically, which defaulted the selector to Command — it now opens on Snippet in the spec's order; and the title only reports `aria-invalid` after the field has been visited, so a fresh form doesn't open outlined in destructive red. Also extracted the `Field` row that was duplicated byte-for-byte between this dialog and `ItemEditForm` into `src/components/dashboard/FormField.tsx`. Tests 80 → 108 unit and 10 → 21 e2e (`e2e/item-create.spec.ts`); the fixture CLI gained `findByTitle`/`removeByTitle` because items created through the UI get cuid ids rather than known ones. Content round-trips verbatim (leading indentation and trailing newline), asserted against the stored row, and every new guard was mutation-checked — including the in-flight test, which holds the server action open with a route delay to assert Submit/Cancel/Close are all disabled. One e2e gotcha worth remembering: the submit button must be located by `type="submit"`, since its accessible name changes to "Creating…" while pending, and `toHaveText` reads untransformed text so CSS-uppercased labels assert in sentence case. Known gaps left unfixed: the free-tier 50-item limit is not enforced (absent from the spec); "New Collection" is still inert; the create dialog's URL input lacks the `type="url"` the edit form has, so the two differ only in the mobile keyboard they raise; creating a type you aren't currently viewing toasts success with no visible list change; and pages still fetch with `DEMO_USER_ID` while the action scopes to `session.user.id`, so created items only appear for the seeded demo login. Completed 2026-08-04.
- 2026-08-04: **Code Editor** — Per `context/features/code-editor-spec.md`. Snippet and command content now renders in Monaco instead of a `<pre>` (display) or `Textarea` (edit); prompts and notes keep both. Installed `@monaco-editor/react` ^4.7 plus `monaco-editor` pinned to **0.52.2** — 0.56 restructured its `exports` map so `monaco-editor/esm/vs/editor/editor.api` (which `@monaco-editor/react`'s own types import) no longer resolves. Two components: `CodeEditor.tsx` owns the chrome (macOS traffic lights, language label + `CopyButton` in the header, `data-code-editor` mode marker) and pulls the surface in via `next/dynamic(..., { ssr: false })`; `CodeEditorSurface.tsx` imports monaco at module scope, so it must never be server-evaluated — client components still get SSR'd, and monaco touches `window` on import. `loader.config({ monaco })` hands the library our bundled copy, which short-circuits `@monaco-editor/loader` before it would fetch from jsDelivr (self-hosted deliberately: the Playwright suite would otherwise gain a network dependency). Imports `edcore.main` + `basic-languages/monaco.contribution` rather than `editor.main`, which keeps every Monarch tokenizer but drops the TS/JSON/CSS/HTML *language services* — those spawn extra workers and would only decorate a snippet store with squiggles. `window.MonacoEnvironment.getWorker` wires the base editor worker via `new Worker(new URL(...))`, which Turbopack bundles (a `turbopack-worker-*.js` chunk is emitted); without it Monaco logs "Could not create web worker". Height is fluid: `onDidContentSizeChange` → `clamp(contentHeight, 56, 400)` with the state write guarded against its current value (unguarded it re-renders → re-measures → oscillates) and `scrollBeyondLastLine: false`, without which the editor always reports a full cap of empty space. `alwaysConsumeMouseWheel: false` so the drawer keeps scrolling at the editor's boundary. Monaco's scrollbar is not CSS-stylable — it renders its own — so it is themed through `defineTheme`'s `scrollbarSlider.*` colors; `defineTheme` also only parses **hex**, so `devstash-dark`/`devstash-light` hardcode the hex equivalents of the `oklch()` tokens in `globals.css` (they land on Tailwind's neutral scale exactly: `--muted` → `#262626` / `#f5f5f5`). Theme follows the app's own switch, not the OS: extracted `useAppTheme()` (`src/components/theme/useAppTheme.ts`) from the `useSyncExternalStore` logic that `ThemeToggle` already had, and adopted it in `ThemeToaster` too rather than writing a third copy. Two pure, unit-tested helpers carry the logic: `usesCodeEditor(itemTypeId)` in `item-form.ts` shares one `CODE_TYPES` set with the `language` field (a test asserts the two can't drift), and `src/lib/code-language.ts` maps free-text language → Monaco id (`bash`/`zsh`/`sh` → `shell`, `tsx` → `typescript`, unknown → `plaintext`) plus a display label (`ts` → "TypeScript", unmapped echoed back trimmed). `FormField.htmlFor` became optional: Monaco is a widget with no labelable control, so a `<label for>` would point at nothing — the row renders a `span` and the editor names itself via `ariaLabel`. Both label kinds carry `data-slot="field-label"` so the create-dialog field-list spec still has one selector. One real bug found by the new e2e layer: Monaco fixes a model's EOL at creation and rewrites later edits to match, so content pasted from a CRLF source was **stored with `\r\n`** — the textarea it replaced normalized that away for free. Fixed with `model.setEOL(LF)` on mount, pinned by a failing-first test. Tests 108 → 129 unit and 21 → 35 e2e (`e2e/code-editor.spec.ts` + `e2e/helpers/code-editor.ts`); every new guard was mutation-checked, including the EOL pin, which both content specs fail without. **Driving Monaco from Playwright was the hard part** and cost several rounds: `fill` never reaches the model, `pressSequentially` gets auto-indented and bracket-completed, and a synthetic `paste` `ClipboardEvent` looked right but was intermittently dropped — instrumenting showed Monaco's own listener running and reading the text while `onDidPaste` never fired and `Cmd+A` produced no selection, i.e. Monaco gates keybinding dispatch *and* paste routing on its internal focus state, which Playwright's DOM-level `toBeFocused()` does not speak for. The helper now calls `editor.executeEdits` (deliberately not `setValue`, which re-detects the EOL from the incoming string and would invert the CRLF assertion) and locates the editor by DOM containment; 4 consecutive full-file runs and 2 full-suite runs are clean. That needs a monaco handle, so `CodeEditorSurface` sets `window.monaco` — which is what Monaco's own AMD loader does for a CDN-loaded copy, so the self-hosted bundle now behaves like the standard one. Also hid Monaco's caret in read-only mode via a `[data-code-editor="read-only"]` rule in `globals.css` (a blinking caret in the drawer reads as editable) and fixed the header at `h-9` so it does not resize when the copy button appears with the first character. Verified in the browser as the demo user across both themes: snippet/command/prompt types, the 400px cap on a 24-line Dockerfile, a one-line command hugging the 56px floor, and the create dialog swapping editor↔textarea as the type changes while carrying the draft across. Two more findings from the review pass: Monaco's context menu is now **off** (`contextmenu: false`) — it offers "Command Palette", which opened a quick-input widget inside the New Item dialog, and it renders inside the editor so the dialog's `overflow-y-auto` clipped it; with it off the browser's native menu gives real Cut/Copy/Paste, and a spec pins the decision. Also confirmed the `bash` → `shell` alias tokenizes for real (header reads "BASH") and that the horizontal slider picks up the same themed colours as the vertical one. A self-review pass then fixed two quality issues: the `56`/`400`/line-height numbers were duplicated between the wrapper (for the placeholder) and the surface (for the clamp) — they now live in `src/lib/code-editor.ts` with `clampEditorHeight`/`estimateEditorHeight`, unit-tested, and kept out of the surface module so importing them cannot drag monaco into the parent chunk; and the surface's `options` literal was rebuilt every render, which made `@monaco-editor/react` call `updateOptions` on every keystroke, so it is now `useMemo`'d on `[readOnly, ariaLabel]`. Known limitations: `wordWrap` is off, so long lines scroll horizontally rather than wrapping (the `<pre>` wrapped); with a selection inside a read-only editor the first Escape is swallowed by Monaco to clear that selection, so closing the drawer takes a second press (left as Monaco's own documented behaviour rather than overriding its keybindings); no IntelliSense or diagnostics by design; the language shown in the header is display-only, still edited through the Language field; monaco adds ~2MB to the client bundle, though only for routes that open an editor; and pages still fetch with `DEMO_USER_ID` while the actions scope to `session.user.id`. Completed 2026-08-04.
- 2026-08-05: **Markdown Editor** — Per `context/features/markdown-editor-spec.md`. Notes and prompts now render in a Write/Preview Markdown editor instead of a `<pre>` (display) or `Textarea` (edit); snippets and commands keep Monaco untouched. Installed `react-markdown` ^10.1 + `remark-gfm` ^4.0. New `MarkdownEditor.tsx` owns the chrome (same `bg-muted` container, `h-9` header and `CopyButton` slot as `CodeEditor`, plus a `data-markdown-editor` mode marker) and, unlike the code editor, is **not** lazy-loaded — `react-markdown` renders on the server, so `ssr: false` would buy nothing and would reintroduce the placeholder-height problem Monaco forced. Read-only mode renders the preview under a static `PREVIEW` label rather than a one-item tablist, which would be a tablist that navigates nowhere; edit mode gets a real two-tab `role="tablist"` opening on Write. Added the project's first `Tabs` primitive (`src/components/ui/tabs.tsx`) over `@base-ui/react/tabs`, styled off `aria-selected` (the attribute the primitive sets itself, so the selected look cannot drift from what assistive tech is told). `activateOnFocus` is opted in at the call site — base-ui defaults to manual activation (arrow to move, Enter to switch), which earns its keep for an expensive panel, but Preview is derived from state already in memory. Typography lives in a `.markdown-preview` block in `globals.css` as plain CSS against the theme tokens: react-markdown emits the tags itself so there is nowhere to hang a class, and Tailwind's preflight means list markers, margins and table borders all have to be re-stated. The one element with a React handle is the anchor, which takes `text-blue-500 hover:underline` from the design system plus `target="_blank" rel="noopener noreferrer"`, matching the drawer's URL block. `rehype-raw` is deliberately absent, so stored HTML stays text and item content is not an injection route. Type gating derives rather than declares: `usesMarkdownEditor(id) = TYPES_WITH_CONTENT.has(id) && !CODE_TYPES.has(id)`, so there is no third set to drift, and a unit test asserts every content type gets exactly one of the two editors. That invariant made the plain-`Textarea` branch in both `ItemEditForm` and `NewItemDialog` provably unreachable, so both were removed (`#item-content` / `#new-item-content` no longer exist); the drawer's `<pre>` fallback stays, since it is still reachable for a link carrying stored content. Height reuses `EDITOR_MIN_HEIGHT`/`EDITOR_MAX_HEIGHT`/`clampEditorHeight` from `code-editor.ts` (doc widened to say the bounds are shared) rather than writing 56/400 down again; the bounds reach CSS as custom properties, **not** interpolated class names — a `max-h-[${n}px]` template compiles to nothing, since Tailwind scans source statically. The Write textarea auto-grows in a `useLayoutEffect` that collapses to `height:auto` before measuring, without which `scrollHeight` never reports less than the height already set and the box could not shrink after a deletion. Both panes share the cap so switching tabs cannot resize the drawer past it. A second `useLayoutEffect` re-fits on **width** changes via a `ResizeObserver` — the review pass caught the box stranded at 148px in a pane that had narrowed to need 212px, since the source wraps and `[value]` alone never re-measures; the observer is width-guarded because it also fires for the height changes the fit itself makes, which would loop. `CodeEditor` gets the equivalent free from Monaco's `automaticLayout`, and the spec asks for behaviour matching it. `spellCheck` stays **on**, deliberately unlike the code editor — this is prose the user is composing, and the `Textarea` it replaced spell-checked it too, so disabling it would have been a silent regression on exactly the two types where it matters. **One real bug found in the browser:** the app themes with a `dark` class and never sets `color-scheme`, so the GFM task-list checkboxes — the only native controls in the preview — painted from the OS preference, rendering an unchecked box as a filled dark square on a light page. Fixed by pinning `color-scheme` on `.markdown-preview` to the app's own theme, chosen over redrawing the checkbox by hand because `::after` on an `input` is not supported in every browser. Tests 129 → 133 unit and 35 → 55 e2e (`e2e/markdown-editor.spec.ts` + `e2e/helpers/markdown-editor.ts`); the fixture CLI gained an optional `itemTypeId` so specs can create a throwaway **note** (seeded with Markdown source and no language), which needed empty-string placeholders since its arguments are positional. Driving this editor is far easier than Monaco — it is a real textarea, so `fill` reaches it and preserves content verbatim. Every new guard was mutation-checked: dropping the 400 cap, the `height:auto` collapse, and the `color-scheme` pin each fail their spec, and `activateOnFocus` was proven by the arrow-key spec failing before it was added. The `color-scheme` check initially appeared vacuous — the reused dev server served stale CSS, the same `reuseExistingServer` gotcha this project hit during Item Delete; with a recompile wait it fails correctly. Verified in the browser as the demo user in both themes across every styling requirement (h1–h6, inline and fenced code, nested lists, task lists, blockquote, table, link, `hr`, escaped raw HTML), plus the create dialog swapping editor↔textarea as the type changes while carrying the draft across, and `npm run db:test` green afterwards. Browser console read on a **restarted** dev server (0 errors / 0 warnings across the prompts list, drawer view mode, edit mode, tab switch, the create dialog and a Monaco snippet); a first read on the long-lived server showed `getTypeIcon is not defined` and Monaco `InstantiationService has been disposed` errors, both hot-reload artifacts from editing files while those components were mounted — `ItemDrawer`/`ItemCard` use `TypeGlyph` and never reference `getTypeIcon`. **Known limitations:** single newlines collapse per standard Markdown — `remark-breaks` was deliberately not added since the spec names only `remark-gfm`, so a note typed as plain text with line breaks reflows into one paragraph (no seeded notes exist and the seeded prompts are already blank-line separated, so no stored data changed appearance); the preview is display-only, so task-list checkboxes are disabled and cannot be ticked; there is no Markdown toolbar or keyboard shortcut for bold/italic; the editor is unaware of the drawer's own scroll, so a preview taller than 400px scrolls within its own box; the app-wide `color-scheme` gap is fixed only inside `.markdown-preview`, leaving other native controls following the OS; and pages still fetch with `DEMO_USER_ID` while the actions scope to `session.user.id`. Completed 2026-08-05.
- 2026-08-19: **Collection Create** — Per `context/features/collection-create` goals. The TopBar's "New Collection" button was inert; it now opens a modal (name required, description optional) mirroring `NewItemDialog`'s structure — same `Dialog` primitive, `closeDisabled` while pending, touched-before-invalid title styling, and `noValidate` since the server Zod schema is the source of truth. New `createCollection` server action in `src/actions/collections.ts` (`auth()` gate, `{ success, data, error }` envelope, owner always taken from the session) calls `createCollection(userId, data)` in `src/lib/db/collections.ts`, which returns a zeroed `CollectionCardData` (a fresh collection is always empty) so the caller needs no second read. Toasts on both outcomes; `router.refresh()` after success pulls the sidebar Recent Collections, dashboard Recent Collections grid, and Collections stat count forward together, verified live in the browser (count went 5 → 6, new card appeared first in both lists, no manual reload). Resolved the "server action vs. API route" question from planning in favor of a server action, matching `createItem` and `context/coding-standards.md`. Making the "every surface reflects it" goal actually true for a *real* signed-in user (not just the seeded demo login) required moving `/dashboard` and `/items/[type]` (plus their layouts) off the `DEMO_USER_ID` constant onto `auth()`'s `session.user.id`, redirecting to `/sign-in` when unauthenticated — those collection lists were previously hardcoded to the demo user regardless of who was signed in. `DEMO_USER_ID` stays in `src/lib/constants.ts` for the seed/test-db/e2e-fixture scripts, which still need a stable id. A code-review pass caught that this made `/items/[type]` newly auth-gated only at the page level, with no matching middleware coverage — `src/proxy.ts`'s `PROTECTED_PREFIXES`/matcher only listed `/dashboard` and `/profile`, unlike the defense-in-depth pattern the 2026-05-26 Profile Page work established (protect at both layers); fixed by adding `/items` to both, confirmed via `curl` (unauthenticated request now 302s to `/sign-in`) and a signed-in browser session still loading `/items/prompts` cleanly. Tests 142 → 150 across a new `src/actions/collections.test.ts` and additions to `src/lib/db/collections.test.ts` (auth/session-id rejection, blank-name rejection, DB-throw handling, owner-scoping, the zeroed-count return shape); `npm run build` and `npm run lint` both clean. Known gaps left unfixed, same as Item Create: the free-tier 3-collection limit is not enforced; there is still no `/collections` route, so "View all collections" 404s.
- 2026-08-19: **Add Item to Collections** — Adds a Collections multi-select to both the New Item dialog and the drawer's `ItemEditForm`, so an item can be added to one or more of the user's collections at create time, with edit-mode membership fully replaceable (checking adds, unchecking removes — same semantics as Tags). New shared `CollectionPicker` component (`src/components/dashboard/CollectionPicker.tsx`) renders a checkbox-chip list styled off the existing type-selector pattern, and — after a review pass caught the group had no accessible name — builds its own `<legend data-slot="field-label">Collections</legend>` rather than being wrapped in `FormField` (whose label is a sibling `<span>`, not associated with a `<fieldset>` the way a `<legend>` is; the Type selector avoids `FormField` for the same reason). Fed by a new `getCollectionOptions(userId)` in `src/lib/db/collections.ts` (id + name only, alphabetical — lighter than `getCollections`' counts/description), threaded through `dashboard/layout.tsx` and `items/layout.tsx` → `DashboardShell` → `TopBar` (for `NewItemDialog`) and → `ItemDrawerProvider` → `ItemDrawer` (for `ItemEditForm`). `ItemCollection` is an **explicit** join model (composite `@@id([itemId, collectionId])` + `addedAt`), unlike the implicit many-to-many `Item.tags`, so `createItem`/`updateItem` in `src/lib/db/items.ts` use `collections: { create: [...] }` on create and `collections: { deleteMany: {}, create: [...] }` on update rather than tags' `connectOrCreate`/`set`. Every submitted `collectionId` is verified as owned by the session user via a new `verifyCollectionIds` helper (`prisma.collection.findMany({ where: { id: { in }, userId } })`) before linking — a foreign or made-up id is silently dropped rather than trusted, mirroring the project's "never trust a client-sent userId" convention. `collectionIds` was added to `ItemDraft`/`buildCreatePayload`/`buildUpdatePayload` in `item-form.ts` and to the shared Zod schema in `actions/items.ts`, unconditionally (not type-gated) since every item type can belong to a collection. Fixing the `CollectionPicker` accessibility gap required updating `e2e/item-create.spec.ts`: the Type-selector label assertion is now scoped to `getByRole("group", { name: "Type" })` since a bare `"fieldset label"` locator would also match the new Collections fieldset, and the three field-label-list assertions now expect `"Collections"` trailing every type. Tests 160 unit (10 new) plus all 13 `item-create.spec.ts` e2e specs green; `npm run build`, `npm run lint`, and `npx tsc --noEmit` all clean. Verified live in the browser as the demo user: created a snippet into two collections (both `ItemCollection` rows inserted, both collections' item counts incremented), opened it in the drawer to see both as read-only badges, edited to uncheck one (membership correctly reduced to the one retained collection, count decremented), and deleted the throwaway item afterward to leave the dev DB clean. Known gaps left unfixed, per the loaded spec: collection detail/list pages remain out of scope (still no `/collections` route) and the free-tier 3-collection limit is still not enforced.
- 2026-08-19: **Collections Pages** — Closes the "no `/collections` route" gap left by Collection Create and Add Item to Collections. New `/collections` (all of the user's collections via `CollectionCard`) and `/collections/[id]` (items in that collection via `ItemCard`) routes, both server components scoped to `session.user.id` with a `/sign-in` redirect when unauthenticated, `force-dynamic`, and a `collections/layout.tsx` mirroring `items/layout.tsx` so both render inside `DashboardShell`. No new `lib/db` logic was needed — `getCollections`, `getCollectionById`, and `getItemsByCollection` already existed from prior work, so the pages just call them; `getCollectionById` returning `null` (missing or foreign id) triggers `notFound()`. `CollectionCard` now wraps its `Card` in a `next/link` to `/collections/${id}` (with a `hover:bg-muted/40` tint matching `ItemCard`'s treatment) — since the card has no interactive children (unlike `ItemCard`'s Copy button), a plain anchor wrap was correct where `ItemCard` needed the `role="button"` `ItemCardTrigger` div instead. This single change makes every existing `CollectionCard` render site (dashboard Recent Collections, the new `/collections` list) clickable with no per-call-site changes. The sidebar's "View all collections" link and its per-collection links already pointed at `/collections` and `/collections/${id}` from earlier work — they now resolve instead of 404ing. Added `/collections` to `src/proxy.ts`'s `PROTECTED_PREFIXES`/matcher for defense-in-depth, matching the pattern established for `/dashboard`, `/profile`, and `/items`. No new unit tests were needed (no new `lib/db`/action logic — only existing exports reused); `npm run build`, `npm run lint`, and all 160 existing tests pass. Verified live in the browser as the demo user: dashboard cards and the `/collections` list both navigate to the right detail page, an unknown collection id 404s, and unauthenticated requests to both routes 302 to `/sign-in`. Known gap left unfixed, same as prior collection work: the free-tier 3-collection limit is still not enforced.
- 2026-08-19: **Collection Edit, Delete & Favorite Affordances** — Added an action bar (Favorite/Edit/Delete) to `/collections/[id]` via a new `CollectionDetailHeader`, and a 3-dot dropdown on `CollectionCard` (dashboard + `/collections` list) via a new `CollectionCardMenu`, both offering the same three actions. Favorite is display-only this pass (disabled button/menu item showing current state, no handler — same convention as the item drawer's "coming soon" Favorite/Pin). Edit opens a modal (`EditCollectionDialog`, mirrors `NewCollectionDialog`) pre-filled with name/description; Delete opens a confirmation (`DeleteCollectionDialog`, mirrors `DeleteItemDialog`) whose copy explicitly states items are unlinked, not deleted — true for free via the schema (`ItemCollection.onDelete: Cascade` only cascades the join row, never `Item`). Added sibling `updateCollection`/`deleteCollection` at both the action (`src/actions/collections.ts`) and db layer (`src/lib/db/collections.ts`), each ownership-checked via `findFirst({ id, userId })` before mutating, mirroring `updateItem`/`deleteItem` exactly. Built the project's first dropdown-menu primitive (`src/components/ui/dropdown-menu.tsx`) over `@base-ui/react/menu`, following the same hand-rolled wrapper pattern as `dialog.tsx`/`alert-dialog.tsx`/`tabs.tsx`. Since `CollectionCard` needed a real `<button>` for the menu trigger (invalid to nest inside the whole-card `<a>` the prior feature added), it reverted to a plain server component and a new client `CollectionCardTrigger` (mirrors `ItemCardTrigger`, `router.push` instead of opening a drawer) took over click-to-navigate at both call sites. **One real bug found via browser verification and fixed in this session**: stopping propagation only on the menu-trigger button wasn't enough — React re-parents portal event bubbling through the *component* tree, not the DOM tree, so a click on "Edit"/"Delete" inside the portaled dropdown popup still reached `CollectionCardTrigger` and navigated away instead of opening the dialog. Fixed by wrapping the trigger, dropdown, and both dialogs in one `stopPropagation` div inside `CollectionCardMenu`. Tests 160 → 179 across `src/lib/db/collections.test.ts` and `src/actions/collections.test.ts`, mirroring the existing `updateItem`/`deleteItem` test shape (ownership checks, not-found, blank-id, db-throw). `npm run build`, `npm run lint`, and the full test suite all pass. Verified live in the browser as the demo user: card-menu Edit/Delete on both surfaces, detail-page Edit/Delete, delete confirmation copy, non-interference between menu clicks and card navigation, and edit/delete round-trips against throwaway collections (seed data left untouched). Known minor limitation: `EditCollectionDialog` is mounted unconditionally (not only-while-open like `ItemEditForm`), so its draft only resets on close — a collection edited from elsewhere while its dialog was never opened could show stale values on first open; doesn't affect the normal single-actor edit flow. Free-tier 3-collection limit enforcement remains out of scope.
- 2026-08-19: **Global Search / Command Palette** — Resolved the loaded spec's one open architectural decision in favor of hand-rolling over `@base-ui/react` rather than installing `cmdk`, matching every other overlay primitive in the codebase. New `src/components/ui/command.tsx` wraps `@base-ui/react/autocomplete`, rendered *inline* (no `Popup`/`Positioner`/`Portal`) inside the project's existing `Dialog` — `ComboboxList` already degrades gracefully without a positioner ancestor, confirmed by reading its source before committing to the approach. `Command`'s `open`/`onOpenChange` are passed straight through from the caller rather than a wrapper default; `CommandPalette.tsx` wires both to the same state as the wrapping `Dialog`. **One real bug found and fixed via live browser testing**: an earlier version forced the Autocomplete root's `open` to a constant `true` with a discarded `onOpenChange`, reasoning that item registration/highlighting need the root "open" while shown — this quietly swallowed the Escape key before it ever reached the wrapping `Dialog`'s own close handler, since Autocomplete's internal Escape-to-close fired, called the no-op, and never bubbled further. Fixed by unifying the two "open" concepts into one piece of state instead of faking a constant. Search logic lives in the pure, unit-tested `src/lib/search.ts` (`searchIndex`): a case-insensitive **subsequence** match (not substring — "gsrch" matches "Global Search"), scored by gap size and how early the first match lands, capped at 8 results per group; an empty query returns nothing rather than the whole index. `getSearchableItems(userId)` (new, in `src/lib/db/items.ts`) fetches id/title/itemTypeId/itemTypeName plus a 140-char preview built server-side from `description ?? content ?? url` — Prisma can't truncate a column in `select`, so the full row is fetched and trimmed down before the narrow shape ever reaches the client, and the query is capped at `take: 500` (`SEARCH_INDEX_LIMIT`) since the palette prefetches the whole index client-side with no per-keystroke round-trip. Collection search reuses the existing `getCollections(userId)` (now called unlimited in all three layouts, with `recentCollections` derived via `.slice(0, 5)` for the sidebar instead of a second query) mapped to `{ id, name, itemCount }`. Threaded through `dashboard/`, `items/`, and `collections/layout.tsx` → `DashboardShell` (all three edited identically, since a route missing the fetch would silently search an empty index) → `CommandPalette`, mounted inside `ItemDrawerProvider` so `useItemDrawer()` is in scope. The Cmd+K/Ctrl+K listener lives in `DashboardShell`, attached on `document` in the **capture phase** so it beats Monaco's own chord handling — verified live with a snippet's Monaco editor focused: the palette opened and Monaco's `.quick-input-widget` never appeared. Selecting an item calls `openItem(id)`; selecting a collection calls `router.push`; both close the palette first via a shared `handleOpenChange` so the two never race. Verified live that opening the palette while the item drawer is already open does **not** need a z-index override or an "inert while drawer open" rule: base-ui's own dialog stacking already renders the most-recently-opened dialog on top (confirmed via screenshot) and routes Escape to the topmost one first, closing just the palette and leaving the drawer beneath it intact. TopBar's inert `<Input placeholder="Search...">` became a real `<button>` (click opens the palette) with a `⌘K` `<kbd>` chip rendered as a sibling rather than placeholder text, per the spec's note that the hint must survive the field becoming a button; the platform-specific hint is hardcoded to `⌘K` (avoids a client/server hydration mismatch from `navigator.platform` detection) while the actual listener accepts both `metaKey` and `ctrlKey`. Tests 191 → 192 unit (`src/lib/search.test.ts` new, `getSearchableItems` cases added to `src/lib/db/items.test.ts`) plus a new `e2e/global-search.spec.ts` (8 specs: open via keyboard, item search → drawer, collection search → navigation, arrow+Enter selection, no-results message, Escape-closes-without-navigating, and opening from `/items/[type]`) reusing `createTestItem`/`removeTestItem` — throwaway items link to the seeded `col_react_patterns` collection for free, so collection search needed no new fixture. `npm run build`, `npm run lint`, `npx tsc --noEmit`, and the full Playwright suite all pass on a **freshly started** dev server (not a reused stale one, per this project's own documented gotcha). One deliberate ranking tradeoff worth flagging rather than hiding: for the query "react", "Refactoring Assistant" ranks above "shadcn/ui" — both are valid subsequence matches and the scorer prefers an earlier first-character match over a tighter one further in, which a unit test pins as intentional rather than leaving it to drift. Known limitations: the fuzzy scorer is a simple gap-and-position heuristic, not a full fuzzy-match library, so ranking quality is "good enough," not tuned; the search index is capped at the 500 most recently updated items per account, so an account past that size won't find its oldest items via the palette; and pages still fetch with `session.user.id` (this feature did not touch the demo-user gap left by earlier work, since `/dashboard`, `/items/[type]`, and `/collections` were already migrated off `DEMO_USER_ID` during Collection Create).
- 2026-08-19: **Pagination** — Per `context/features/pagination-spec.md`. `/items/[type]` and `/collections/[id]` now paginate instead of rendering every row, with a numbered pager plus prev/next at the bottom of each grid. Logic lives in the pure, unit-tested `src/lib/pagination.ts`: `parsePageParam` treats anything that isn't a positive integer (`0`, negative, non-numeric, a repeated `?page=`) as page 1 rather than erroring; `getPagination(requestedPage, total, perPage)` clamps a page past the end down to the last page, so deleting the only item on page 3 lands on page 2 instead of an empty grid; `getPageLinks` windows the numbered links around the current page with `PAGE_GAP` ellipsis markers once the page count exceeds `MAX_PAGE_LINKS` (7), always keeping first/last visible and absorbing a gap that would hide only one page; `pageHref` gives page 1 a bare path so the canonical URL carries no query string. New `Pagination.tsx` component renders the links plus prev/next, greying out (and `aria-disabled`ing) whichever end is unavailable. `src/lib/constants.ts` gained `ITEMS_PER_PAGE`/`COLLECTIONS_PER_PAGE` (21 — divides evenly into the 3-column `lg` grid with no ragged last row) and `DASHBOARD_COLLECTIONS_LIMIT`/`DASHBOARD_RECENT_ITEMS_LIMIT` (6/10, replacing the magic numbers previously inlined in `dashboard/page.tsx`). Fetches now request only the page's window instead of fetch-all-then-slice: `getItemsByType`/`getItemsByCollection` in `src/lib/db/items.ts` and `getCollections`/`getItemsByCollection` (collections-page listing) in `src/lib/db/collections.ts` all take an optional `PageWindow { skip?, take? }` (an `undefined` window still returns every row, since Prisma drops undefined `skip`/`take` — needed by the unpaginated dashboard/search callers), paired with new `countItemsByType`/`countItemsByCollection` counts for the page total. Tests 192 → 227 (35 new): `pagination.test.ts` covers every helper's edge cases (clamping, gap-absorption, repeated query params) plus a new Playwright suite `e2e/pagination.spec.ts` (numbered links, prev/next disabled state at both ends, direct navigation via `?page=`, out-of-range page clamping) — the fixture CLI (`scripts/e2e-item-fixture.ts`) and `e2e/helpers/items.ts` gained bulk-create/cleanup helpers so a spec can seed enough throwaway items to force multiple pages without touching seed data. `npm run build`, `npm run lint`, and the full unit + e2e suites all pass. Verified live in the browser as the demo user: numbered pager and disabled prev/next at page 1 and the last page on both `/items/[type]` and `/collections/[id]`, direct URL navigation to a mid-range page, and the dashboard's fixed-size sections unaffected. Known gap left unfixed: the `/collections` list page (all of a user's collections, not a single collection's items) was not paginated — out of scope per the loaded spec, which only named `/items/[type]` and `/collections/[id]`.
- 2026-08-19: **Settings Page** — New auth-gated `/settings` route, protected the same way as `/profile` (added to `PROTECTED_PREFIXES`/`config.matcher` in `src/proxy.ts` plus a page-level `redirect("/sign-in?callbackUrl=/settings")`). Moved the two account-action cards off `/profile` and onto it: `ChangePasswordForm` (still gated on `User.password` not null, so GitHub-OAuth-only accounts don't see it) and `DeleteAccountDialog` (needs the user's `email` for the type-to-confirm gate) — both moved from `src/components/profile/` to `src/components/settings/` as clean git renames with no content changes, since the underlying `POST /api/auth/change-password` and `DELETE /api/auth/delete-account` routes are session-based and needed no changes. `/profile` now renders only `ProfileUserInfo` + `ProfileStats` (removed the now-unused `password` select field and `hasPassword` derivation). Added a "Settings" link to the sidebar `UserMenu` dropdown, between Profile and Sign out. Verified live in the browser end-to-end on a throwaway registered account (not the seeded demo user, since delete-account is destructive): change-password round-trip (old password rejected after the change, new password signs in), and delete-account's confirm gate (stays disabled on a wrong email, enables on the exact email, deletes and signs out, and the account can no longer sign in afterward). `npm run build`, `npm run lint`, and all 227 existing tests pass — no new `lib/db`/action logic was added, so no new unit tests were needed.
- 2026-08-19: **Editor Preferences Settings** — Per `context/features/editor-settings-spec.md`. Added a `editorPreferences Json?` column on `User` (migration `20260819103146_add_editor_preferences`, applied via `prisma migrate dev` against the Neon dev branch) and an "Editor preferences" card on `/settings` with Font size / Tab size / Editor theme dropdowns plus Word wrap / Minimap switches, auto-saving on every change with a success/error toast. `src/types/editor-preferences.ts` defines the shape and option lists (font sizes capped at `12/13/14/16` — `EDITOR_LINE_HEIGHT` in `code-editor.ts` is a fixed 20px, and larger sizes crowd the line box); `src/lib/editor-preferences.ts` holds a Zod schema (`z.union` of `z.literal`s, not `z.number().refine()`, so the inferred type stays the literal union instead of widening to `number`) and `parseEditorPreferences`, which falls back to `DEFAULT_EDITOR_PREFERENCES` for `null`, a stale/partial shape, or an out-of-range value — the column is freeform JSON, so a hand-edited or older-shape row must not reach Monaco with a bad option. `src/lib/db/user.ts` (`getEditorPreferences`/`updateEditorPreferences`) and `src/actions/editor-preferences.ts` (`auth()` gate, Zod-validated, `{ success, data, error }` envelope) follow the existing collections/items action pattern exactly. **The theme-reconciliation call flagged in this file's Notes**: `vs-dark` (the default) reproduces the editor's original behavior exactly — it keeps following the app's own light/dark toggle (`devstash-dark`/`devstash-light`) via `useAppTheme()` — while explicitly picking `monokai` or `github-dark` always applies that theme regardless of the app's light/dark state, since a light-mode user picking Monokai and seeing nothing change would be worse than a visible switch; verified live in both directions (Monokai/GitHub Dark override in light mode; `vs-dark` still flips with the app theme). Added hand-picked hex palettes (Monaco's `defineTheme` only parses hex) with `scrollbarSlider.*` keys for both new themes, since Monaco renders its own non-CSS-stylable scrollbar. `EditorPreferencesContext` (`src/components/editor-preferences/EditorPreferencesContext.tsx`) is mounted once per `DashboardShell` (dashboard/items/collections layouts, alongside `ItemDrawerProvider`) and again locally on `/settings`; `useEditorPreferences()` throws outside a provider rather than defaulting silently, so a layout that forgets to fetch/thread `editorPreferences` fails loudly instead of quietly ignoring the user's settings. Its `updatePreferences` avoids firing the save inside a `setState` functional updater (React can invoke those twice in dev StrictMode) by mirroring state into a ref and computing the merged value before calling `setState` and the server action separately, and reverts to the previous value if the save fails so the UI never claims a setting persisted when it didn't. `CodeEditor`/`CodeEditorSurface` now read `fontSize`/`tabSize`/`wordWrap` (mapped to Monaco's `"on"/"off"`, not boolean)/`minimap`/theme from context instead of the old hardcoded `BASE_OPTIONS` values, with every one added to the `options` `useMemo`'s deps (a miss there means a saved preference silently stops taking effect on an already-mounted editor). Added `src/components/ui/switch.tsx` (thin `@base-ui/react/switch` wrapper, following the project's existing primitive pattern) but deliberately skipped a full `@base-ui/react/select` wrapper for the three dropdowns — a plain `<select>` styled like `Input` matched the lower-risk native-element precedent already set by the New Item type picker. Tests 227 → 254 (`editor-preferences.test.ts` covers `parseEditorPreferences` fallbacks plus a font/tab-size parity check against the Zod schema so the option list and validator can't drift silently; `db/user.test.ts` and `actions/editor-preferences.test.ts` mirror the existing collections test shape). `npm run build`, `npm run lint`, the full unit suite, and the `code-editor`/`item-create`/`markdown-editor` Playwright suites (46 specs) all pass — the last of those exercises `CodeEditor` heavily and confirmed the prop rewiring didn't regress editing, height-clamping, or CRLF normalization. Verified live in the browser as the demo user: default values render correctly, changing theme/minimap auto-saves and survives a reload, the drawer's Monaco editor picks up Monokai + minimap immediately, and the `vs-dark`-follows-app-theme behavior holds in both light and dark. Known gap: no live sync between an open dashboard tab and a settings change made in another tab/page load — each `EditorPreferencesProvider` instance only reads its initial value once per navigation, matching how other per-user settings in this app already behave (no cross-tab push).
- 2026-08-19: **Favorites Page** — Per `context/features/favorites-spec.md`. New auth-gated `/favorites` route (`favorites/layout.tsx` mirroring `items/layout.tsx`/`collections/layout.tsx` so `DashboardShell`/`ItemDrawerProvider` are in scope, plus a page-level redirect) listing all of a user's favorited items and collections as a compact, monospace, terminal-style list rather than cards. Added `getFavoriteItems` (`src/lib/db/items.ts`) and a sibling `getFavoriteCollectionsList` (`src/lib/db/collections.ts`, alongside the pre-existing sidebar-only `getFavoriteCollections`), both sorted `updatedAt desc, id desc` and unit-tested. Each row shows a type icon, title, a type badge (`Collection` + `Folder` icon for collections, since they have no single type), and a date labelled `upd YYYY-MM-DD` rather than implying a true favorite timestamp — there is no `favoritedAt` column. Items open the `ItemDrawer` via `ItemCardTrigger`, which gained an optional `className` prop so the same client trigger serves both the card grid and this list's row styling; collections are a plain `<Link>` to `/collections/[id]` (no interactive children to conflict with anchor nesting). Sections are omitted individually when empty, with one unified empty state when there are no favorites at all. Added a star-icon `Link` button to the `TopBar` pointing at `/favorites`, and repointed `SidebarNav`'s dead `/dashboard/favorites` link to the new route. `/favorites` added to `proxy.ts`'s `PROTECTED_PREFIXES`/matcher. Decision: unfavoriting from this page is out of scope — it's read-only, since the drawer's and collection menu's own Favorite affordances are still inert placeholders app-wide and toggling should land as one feature covering all three surfaces at once. Initially wired the star button as a base-ui `Button` with `render={<Link/>}`, which triggered a console warning (`useButton`'s `role="button"` override hides the anchor's native link semantics) — reworked to a plain `<Link>` styled with the shared `buttonVariants()` instead. Tests 248 (10 new) across `items.test.ts`/`collections.test.ts`; `npm run build`, `npm run lint`, and the full suite all pass. Verified live in the browser as the demo user: star button navigation, item row opening the drawer, collection row navigating to its detail page, and no console errors.
- 2026-08-19: **Favorite Toggle** — Turned the disabled "Favorite — coming soon" placeholders into real toggles across every surface that had one: the item drawer's action bar, the collection detail page's header, the collection card's 3-dot dropdown menu, and the trailing star on `ItemCard`/`CollectionCard`. Added `toggleItemFavorite(userId, itemId)`/`toggleCollectionFavorite(userId, collectionId)` to `src/lib/db/items.ts`/`src/lib/db/collections.ts` (ownership-checked via `findFirst` before flipping `isFavorite`, mirroring `updateItem`/`deleteItem`), plus matching server actions with the standard `{ success, data, error }` envelope. Two new client hooks, `useItemFavorite`/`useCollectionFavorite` (`src/components/dashboard/`), hold optimistic local state that resyncs from the `isFavorite` prop via the render-time "adjust state" pattern (not a `useEffect`, to satisfy `react-hooks/set-state-in-effect`) and call `router.refresh()` on success; `ItemFavoriteButton`/`CollectionFavoriteButton` wrap them as a `Button` + `Star` (each `stopPropagation()`s so a click doesn't also open the drawer or navigate), while `CollectionCardMenu` uses the hook directly since its Favorite affordance is a `DropdownMenuItem`. A first pass missed the `/favorites` page — it renders a separate, always-favorited `FavoriteItemRow`/`FavoriteCollectionRow` shape, not `ItemCard`/`CollectionCard` — so both rows were also wired up; `FavoriteCollectionRow` had to move off its plain `<Link>` wrap onto `CollectionCardTrigger` (a real `<button>` can't nest inside an `<a>`), which gained the same optional `className` override `ItemCardTrigger` already had so the row keeps its compact styling. Unfavoriting from either row needs no special-case removal logic — the row just disappears on the toggle's own `router.refresh()`, since the page's query only ever selects favorited rows. Known, accepted side effect: toggling always bumps `updatedAt` (Prisma's `@updatedAt` fires on any `.update()`, with no per-field opt-out short of raw SQL), so favoriting a collection also reorders it to the top of "Recent Collections" even though no content changed — confirmed live and left as-is rather than bypassing the ORM's update path. Tests 266 (18 new across `lib/db`/`actions`, item + collection); the ownership guard was mutation-checked (disabling it fails "refuses to toggle an item the user does not own" as expected). `npm run build`, `npm run lint`, and the full existing 78-spec Playwright suite all pass on a freshly restarted dev server (checked first for locator drift from the rewritten drawer/card markup — none found). Verified live in the browser as the demo user across all five surfaces (card grid, drawer, collection header, collection dropdown menu, both `/favorites` row types), confirming stats counts and the sidebar Favorites section stay in sync, then restored the seed's original favorites (2 collections, 2 items) via `npm run db:test`. One transient Neon pooled-driver websocket error surfaced during a rapid test burst — unrelated to the change, self-recovered, and did not recur.
- 2026-08-19: **Favorites Page — Client-Side Sorting** — Added a "Sort by" control (Date/Name/Item Type) to `/favorites`. New pure, unit-tested helpers `sortFavoriteItems`/`sortFavoriteCollections` in `src/lib/favorites-sort.ts` handle each key; Collections have no `itemTypeId`, so "Item Type" is a documented no-op there, leaving them in their existing `updatedAt desc` order. The Items/Collections section rendering moved from `src/app/favorites/page.tsx` into a new `"use client"` `FavoritesList` (`src/components/dashboard/`) that owns the sort state and re-orders the already-fetched arrays via `useMemo`, so `page.tsx` stays a server component doing the single initial fetch. Default sort on load is "Date", matching the existing query order, so nothing changes visually until the user picks a different option. Tests 254 → 274 (20 new in `favorites-sort.test.ts`, covering each sort key with fixtures deliberately distinct across name/date/type so ordering can't coincidentally pass); `npm run build` and `npm run lint` both clean. Verified live in the browser as the demo user: switching sort options re-renders instantly with zero network requests and no console errors, confirming the sort is genuinely client-side rather than a `router.refresh()` round-trip.
- 2026-08-19: **Favorites Page Type Colors** — Colors the item title text and type `Badge` on the `/favorites` items list to match that type's existing icon color (blue for Snippet, purple for Prompt, orange for Command, etc.), reusing `src/lib/type-colors.ts` rather than a new one-off mapping. Added `getTypeBadgeClass(typeId)` (border + text classes, e.g. `border-blue-500/40 text-blue-500`) alongside the existing `getTypeTextClass`/`getTypeDotClass`/`getTypeLeftBorderClass`. Scope was `FavoriteItemRow` only — the Collections rows have no single type and are unaffected. Tests 274 → 277 (unit tests for the new helper's known-type and fallback cases); `npm run build` and `npm run lint` both clean. Verified live in the browser as the demo user: prompt and snippet rows render their title and badge in the matching color, no console regressions.
- 2026-08-19: **Favorites Page Sort Direction** — Added an asc/desc direction toggle next to the existing "Sort by" `Select` (Date/Name/Item Type) on `/favorites`. `src/lib/favorites-sort.ts` gained `FavoritesSortDirection`, `DEFAULT_SORT_DIRECTION` (Date → newest first, Name/Item Type → A→Z), and `getSortDirectionLabel`; `sortFavoriteItems`/`sortFavoriteCollections` take an optional third `direction` param defaulting to the key's natural direction, so the existing 2-arg call sites and tests kept passing unmodified. `FavoritesList.tsx` owns `direction` state alongside `sortKey` — changing the sort key resets direction to that key's default rather than carrying over an unrelated direction — and renders a small icon `Button` (`ArrowUp`/`ArrowDown`) that flips it, with `aria-label`/`title` describing the current direction ("Newest first", "A to Z", etc). Tests 277 → 283 (6 new: descending-name/ascending-date cases for both sort functions, plus `getSortDirectionLabel`); `npm run build` and `npm run lint` both clean. Verified live in the browser as the demo user: toggling direction on Date flips newest/oldest first for both Items and Collections, switching to Name resets the toggle to A→Z, and selecting Name then clicking the toggle correctly reverses to Z→A.
- 2026-08-19: **Pinned Items** — Per `context/features/pinned-spec.md`. Turned the item drawer's disabled "Pin — coming soon" placeholder into a real toggle and made pinned items sort to the top of the paginated listings. Added `toggleItemPin(userId, itemId)` to `src/lib/db/items.ts` (ownership-checked via `findFirst({ id, userId })` before flipping, returning `null` for a missing or foreign item rather than coupling to Prisma's `P2025`) and a matching `toggleItemPin` server action with the standard `{ success, data, error }` envelope — both mirroring `toggleItemFavorite` exactly. New `useItemPin` hook + `ItemPinButton` follow the `useItemFavorite`/`ItemFavoriteButton` pair, including the render-time "adjust state" prop resync (not a `useEffect`, per `react-hooks/set-state-in-effect`) and `stopPropagation()` so a click doesn't also open the drawer. **Two deliberate divergences from the Favorite pattern**, both resolved against the spec rather than the precedent: (1) the spec asks for a toast on *success as well as error*, where favorite is silent on success — pinning reorders listings the user may not be looking at, so "Item pinned"/"Item unpinned" earns its keep; and (2) `isPinned` was added to `ItemCardData`/`ITEM_SELECT` so `ItemCard` can render a **static** `Pin` indicator (`role="img" aria-label="Pinned"`, no `Button` wrapper, conditional on `isPinned` rather than an always-present ghost) — the spec explicitly keeps the card's pin non-interactive, unlike its favorite star. That indicator is suppressed in one place via a `showPinIndicator` prop: the dashboard's own "Pinned Items" section, where every card is pinned by definition, so the badge adds nothing and would make each card announce "Pinned" inside a heading that already says so. Verified live that the section renders 1 card / 0 indicators while `/items/snippets` still marks the pinned row. Adding it to the shared card shape let `ItemDetailData`'s own `isPinned` declaration and `ITEM_DETAIL_SELECT`'s redundant `isPinned: true` both go away, since detail extends card. Pinned-first ordering is `orderBy: [{ isPinned: "desc" }, { updatedAt: "desc" }, { id: "desc" }]` in `getItemsByType` and `getItemsByCollection` **only** — in the query, not after it, so it stays correct across paginated windows. Four listings were deliberately left alone and a unit test pins each exclusion: `getRecentItems` ("Recent" means recently touched, and a permanent pinned-first key would park pinned items at its head and crowd out the activity the section exists to show — note this does *not* stop overlap with the Pinned section above it, since a pin bumps `updatedAt` and surfaces the item in Recent anyway; the review pass caught that the original "would show the same rows twice" rationale was self-defeating), `getFavoriteItems` (`/favorites` re-sorts client-side by the user's chosen key, discarding DB order), `getPinnedItems` (every row is pinned — a no-op), and `getSearchableItems` (ranked by fuzzy score). `getPinnedItems` backs the dashboard section the spec names directly, so its order was decided rather than defaulted: it stays `updatedAt desc`, which — because a pin toggle bumps `updatedAt` — puts the item the user just pinned at the front. There is no `pinnedAt` column to sort by instead, and recency is the closest honest stand-in for "recently pinned". **Fixed a latent staleness bug rather than inheriting it:** `ItemDrawerProvider` holds fetched `ItemDetailData` in both a per-session cache and its live `item` state, and the toggle button owns its optimistic value only while mounted — so a flag toggled in the drawer left both copies pre-toggle. Added `handleFlagToggled(itemId, patch)`, which folds the new boolean into the cache entry *and* the live state (id-guarded, since the user may have opened a different item by then). Threaded as `onFlagToggled` through `ItemDrawer` to both buttons via a shared `ItemFlagPatch` type exported from `lib/db/items.ts` (defined there rather than in the provider, which would have made the buttons import from a module that transitively imports them). **Favorite had the identical bug at the same call site and is fixed incidentally** — but only *at that call site*: `useItemFavorite` gained a third optional param, and the star on `ItemCard`/`FavoriteItemRow` still passes no callback, so favoriting from a card leaves the drawer's cached detail stale. Those two are server components and structurally cannot pass one; routing the callback through `ItemDrawerContext` instead of props would fix all sites at once, and is left as a follow-up rather than widened into this feature. Pin is unaffected — it is drawer-only by spec. The first implementation only *evicted* the cache entry, which fixed reopening but not editing — the review pass caught the rest. Tests 303 unit (19 new across `src/lib/db/items.test.ts` and `src/actions/items.test.ts`) plus a new `e2e/item-pin.spec.ts` (8 specs: enabled-not-placeholder, label flip + both toasts, card indicator absent-then-present, no second toggle on the card, pinned-first ordering, state surviving a cancelled edit, and state surviving a reopen). Every new guard was mutation-checked — the db ownership check, the pinned-first key, and the action's session and blank-id guards each fail their unit test when removed, and both headline e2e specs fail when their mechanism is disabled. The ordering spec is written to be non-vacuous on purpose: it pins an item and *then* creates a newer one, so recency alone would put the unpinned item first. Verified live in the browser as the demo user across `/items/[type]`, `/collections/[id]` and `/dashboard`: pin from the drawer reorders the list and populates the dashboard's Pinned Items section via `router.refresh()` with no manual reload, both toasts fire, the reopened drawer shows fresh state, and the console is clean (0 errors / 0 warnings). **The review pass found one real bug in the first implementation, fixed here with a failing test written first:** `ItemViewMode` unmounts entirely while editing, so pin → Edit → Cancel remounted the action bar from the provider's still-stale `item` and offered to "Pin" an item that was already pinned (confirmed live — the card carried its Pinned badge and had sorted to the top while the drawer disagreed). Evicting the cache could not fix that; only patching the live state does. So the mechanism changed from evict to patch, which also keeps the cached reopen instant instead of paying a refetch. Both halves of the patch are independently mutation-checked and each is proven by exactly one spec: dropping the `setItem` patch fails only the cancelled-edit spec, dropping the cache patch fails only the reopen spec. The favorite path was then verified live on the same sequence (favorite → Edit → Cancel now reads "Unfavorite"), and `npm run db:test` caught a pinned row left behind by the manual testing, which was restored before finishing. Seed state was restored afterwards and `npm run db:test` is green. The full 85-spec Playwright suite was run end to end on a **freshly restarted** dev server with `.next` cleared, per this project's own documented staleness gotcha — a first run on the HMR'd server showed 4 failures that all passed in isolation and did not reproduce clean. Two full runs each ended with a single non-assertion failure in a `tsx` fixture subprocess (`[object ErrorEvent]` from the Neon pooled driver), the same transient websocket class the Favorite Toggle entry records; both specs pass on re-run, and the one leaked throwaway item was removed with `npm run db:test` green afterwards. Also updated a now-false comment in `e2e/item-delete.spec.ts` that still called Favorite and Pin "deliberate placeholders". **An independent adversarial review then found two more real problems, both fixed here.** (1) Patching only the boolean left `updatedAt` stranded: the toggle bumps it server-side, the drawer footer renders `Updated {formatIsoDate(item.updatedAt)}` from the same object, and nothing evicted the entry — so pinning a snippet last touched in April left the footer reading April for the rest of the session while the listing had already reordered on the new timestamp. Rather than fall back to eviction (which fixes the footer but reintroduces the cancelled-edit bug), both toggles now `select: { <flag>: true, updatedAt: true }` and return `{ <flag>, updatedAt }`; the actions serialize it to an ISO string (matching `ItemDetailData`, so the client never receives a `Date`) and the hooks patch both fields. That keeps every copy accurate with no refetch. `ItemFlagPatch` widened to `Partial<Pick<ItemDetailData, "isFavorite" | "isPinned" | "updatedAt">>`. (2) `getPinnedItems` was unbounded, which was harmless while only the seed could pin two rows but not once any user can pin without limit — it sits above two already-capped sections and would have put every pinned item's `content` on the wire on each `force-dynamic` render. Added `DASHBOARD_PINNED_ITEMS_LIMIT = 6` (matching `DASHBOARD_COLLECTIONS_LIMIT`) as the default `limit` param, with tests for both the default and an explicit override. The review also correctly flagged that the `getRecentItems` exclusion rationale was self-defeating and that the history entry still described the superseded eviction design — both corrected above. Its remaining findings were judged not worth acting on: the ~90 duplicated lines between the pin and favorite hook/button pairs are what "follow the Favorite Button pattern" asked for, and no composite index backs the new `orderBy` but none backed `updatedAt` ordering before either, so there is no regression.

  **Known limitations:** toggling bumps `updatedAt` (Prisma's `@updatedAt` fires on any `.update()`), so pinning also reorders the item within the unpinned group and to the top of Recent — the same accepted side effect the Favorite Toggle entry documents, confirmed live and left alone rather than bypassing the ORM; pinning is drawer-only, so there is no per-card action and no way to pin from `/favorites`' compact rows; an item pinned out of band (another tab/device) can still show a cached ghost, since eviction only fires for toggles made through this drawer; collections have no pin (spec says items only); and the pre-existing unrelated `src/lib/db/user.test.ts` type error and the dead `/dashboard/recent` sidebar link both remain. Completed 2026-08-19.
- 2026-08-19: **Favorite Toggle — Drawer Cache Sync via Context** — Closed the known limitation carried into the Pinned Items entry: favoriting from a *card* left the drawer's cached detail stale, because `ItemDrawerProvider`'s `handleFlagToggled` was threaded down as an `onFlagToggled` prop and `ItemCard`/`FavoriteItemRow` are server components that structurally cannot pass one. Routed the callback through context instead, which fixes every call site at once. Extracted the context into a new `src/components/dashboard/ItemDrawerContext.ts` rather than adding to `ItemDrawerProvider` — the provider renders `ItemDrawer`, which renders the flag buttons, whose hooks now need the context, so keeping it in the provider would have closed a real import cycle (`Provider → Drawer → FavoriteButton → useItemFavorite → Provider`); same reasoning that already parks `ItemFlagPatch` in `lib/db/items.ts`. The module owns the context, the existing throwing `useItemDrawer()` (its two importers, `CommandPalette` and `ItemCardTrigger`, were repointed at it rather than given a re-export shim, so there is one canonical source), and a new **non-throwing** `useItemFlagSync()` returning `useContext(...)?.notifyFlagToggled` — deliberately unlike `useItemDrawer`, since a favourite star is self-contained and must keep working on a surface rendered outside `DashboardShell`, where there is simply nothing to patch. `ItemDrawerContextValue` gained `notifyFlagToggled`; `handleFlagToggled` is `useCallback(…, [])`, so adding it to the `useMemo` keeps the value referentially stable and the "every card trigger re-renders when the drawer opens" concern the memo exists for still holds. Both `useItemFavorite` and `useItemPin` dropped their optional third param and read the callback from context, and `onToggled`/`onFlagToggled` were removed from `ItemFavoriteButton`, `ItemPinButton`, `ItemDrawer` and `ItemViewMode` — pin was routed through context too even though it is drawer-only, because leaving a prop override beside the context read would have meant two live mechanisms doing the same job. No server logic changed (`npm test` stayed 303/303 as a no-regression check); the proof is a new `e2e/item-favorite.spec.ts` (2 specs). **Both specs had to be written to be non-vacuous:** the obvious sequence — favorite from a card, open the drawer, assert "Unfavorite" — passes without the fix, because `openItem` only serves stale data when a cache entry already exists and a cold open falls through to the API for the truth. So each spec opens the drawer *first* to seed the cache with `isFavorite: false`, closes it, toggles from the card, then reopens with **no navigation in between** (a reload would wipe the `useRef` cache and hide the bug). Both fail on current `main` and both fail again when only the `useItemFlagSync()` read in `useItemFavorite` is neutered. The second spec adds the Edit → Cancel path, which exercises `ItemViewMode` remounting from the provider's live state rather than the cache. No spec covers the `/favorites` row separately, deliberately: `FavoritesList` wraps every row in `ItemCardTrigger`, which calls the *throwing* `useItemDrawer()`, so that page demonstrably has a provider mounted or it would already be crashing — and the row runs the identical `ItemFavoriteButton` → `useItemFavorite` → `useItemFlagSync()` chain, so a second spec would only re-test the same wiring. It was verified live instead (below). One incidental finding while writing them: the fixture's default title (`E2E throwaway <sanitized spec name>`) is a single unbreakable word long enough to overflow the card's grid track, and the neighbouring card then paints over the star — making it unclickable for reasons unrelated to the test — so the specs pass a short, space-separated title instead, with a comment recording why. `npm run build`, `npx tsc --noEmit` (only the pre-existing `src/lib/db/user.test.ts` error remains), and `npm run lint` (0 errors in `src/`/`e2e/`) all clean, and the full Playwright suite was run end to end on a freshly restarted dev server with `.next` cleared, per this project's own documented staleness gotcha — `e2e/item-pin.spec.ts` in particular is the regression net for removing the prop threading. Verified live in the browser as the demo user on both server-component call sites, each using the discriminating sequence rather than a cold open: on `/items/prompts` the drawer was opened to seed the cache, closed, the card star clicked, and the reopened drawer showed the flipped label with `Updated` moved to today (pre-fix it read the old label and the April date — proving both halves of the patch, the boolean and the timestamp, reach the cached detail); on `/favorites` the same sequence was run against a row, which removes itself on unfavorite, so the drawer was reopened through the Cmd+K palette — same page, provider still mounted — and likewise read the fresh state. The drawer's own toggle was then used to restore the seeded favourite, and `npm run db:test` is green. Console was clean across all of it (0 errors, 0 warnings), apart from one transient Neon pooled-driver websocket failure ("Received network error or non-101 status code") on a `router.refresh()` — the same class this file records twice before; the DB write itself had landed, and it did not recur. **Known limitations:** an item flagged out of band (another tab or device) can still show a cached ghost, since the sync only fires for toggles made in this tab; collection favourites (`useCollectionFavorite`) need no equivalent, as collections have no drawer or detail cache; and the `updatedAt` bump on any toggle still reorders listings, the accepted side effect the Favorite Toggle entry documents. Completed 2026-08-19.
- 2026-08-20: **Homepage Mockup** — Per `context/features/homepage-mockup-spec.md`. Built a standalone marketing homepage at `prototypes/homepage/` (`index.html`, `styles.css`, `script.js`) — a **static prototype, not a Next.js route**, so no Tailwind v4 / ShadCN / server-component standards apply and no dependencies were added. Fixed top nav (logo, Features/Pricing, Sign In / Get Started) that opacifies on scroll; hero with the "Stop Losing Your Developer Knowledge" gradient headline above a three-part chaos-to-order visual — a "Your knowledge today..." field holding 8 inline-SVG source icons (Notion, GitHub, Slack, VS Code, browser tabs, terminal, text file, bookmark), a pulsing transform arrow, and a "...with DevStash" dashboard preview (sidebar nav + card grid with colored top borders). The icons are animated in a single `requestAnimationFrame` loop: random drift, wall bouncing, rotation/scale pulsing via CSS custom properties (`--scale` etc., so the transform composes without the JS having to rebuild the whole string), and cursor repulsion driven by a `pointermove` listener that converts `clientX/Y` into field-relative coordinates and deactivates on `pointerleave`/`blur` so a cursor parked off-page stops pushing. Below the fold: 6 feature cards on the item-type accent palette, a two-column AI section (Pro badge + capability checklist beside a code-editor mockup demoing AI-generated tags), Free vs Pro pricing with a monthly/yearly toggle (the $72/year option and its "save $24" note are swapped in from JS rather than duplicated in markup), a CTA section, and a footer whose copyright year comes from `getFullYear()`. Sections fade in on scroll via `IntersectionObserver`; at mobile widths the chaos/arrow/dashboard row stacks and the arrow rotates 90° to point down. Verification was browser-based by design — the prototype adds no server actions, `lib/db` helpers, or `src/lib` utilities, so per the project's testing scope no Vitest tests were warranted; `npm test` (303/303) and `npm run build` were run to confirm the untouched app is unaffected. **Resolved the spec's open palette question in favor of the spec, and it is a real divergence worth tracking:** the mockup uses Prompt `#f59e0b` (amber), Command `#06b6d4` (cyan), and Note `#22c55e` (green), where the running app's `src/lib/type-colors.ts` uses purple, orange, and yellow for those same three types — so marketing and product currently disagree on three of seven type colors. Whether the app should align to the mockup, or the mockup to the app, is left open. Also note the mockup's "URL" type is the app's "Link". Incidentally added `/graphify-out` to `.gitignore` (2.2MB of local knowledge-graph tool cache that was sitting untracked). **Known limitations:** the page is a prototype only — nothing links to it from the app, it is not served by Next.js, and every CTA/nav/footer link is an inert `#` or in-page anchor; the pricing toggle changes displayed copy but is wired to no checkout; the dashboard preview is hand-built markup, not the real `/dashboard`, so it will drift as the app changes; and the animation runs unconditionally, with no `prefers-reduced-motion` opt-out.
- 2026-08-20: **Homepage** — Per `context/features/homepage-spec.md`. Turned the static prototype in `prototypes/homepage/` into the real marketing homepage at `/`, replacing the `<div>DevHub</div>` stub in `src/app/page.tsx`. Server components by default under `src/components/home/`, with exactly four client components: `SiteNav` (scroll-opacity listener), `Reveal` (scroll fade-in), `PricingToggle` (billing cycle) and `ChaosField` (the hero's `requestAnimationFrame` drift/bounce/cursor-repulsion field). `ChaosField` keeps particle state in refs and writes `--tx/--ty/--rot/--scale` straight to the DOM, so the 60fps loop never re-renders React; the `prefers-reduced-motion` gate, the `dt` cap and the debounced resize are ported from the prototype, and the frame plus all four listeners are torn down on unmount. Content is DRY — typed consts in `src/lib/home-content.ts` rendered by one `.map()` each, plus shared `SectionHeading`, `CheckItem` and `CtaLink`. CTAs are plain `next/link` styled with `buttonVariants()`, never `Button render={<Link/>}`, which overrides the anchor's semantics with `role="button"` and warns (the convention the Favorites Page entry records). **Palette decision, which deliberately reverses `homepage-mockup-spec.md`:** the app's `src/lib/type-colors.ts` wins over the mockup's hexes, which disagreed on five of seven types (prompt amber-vs-purple, command cyan-vs-orange, note green-vs-yellow, link indigo-vs-emerald, file slate-vs-gray); added `getTypeTopBorderClass` and `getTypeSoftBgClass` beside the existing four helpers, with matching known/unknown/null tests, so marketing and product cannot drift and no new hex entered the palette. **Theme decision:** the page follows the app theme through tokens rather than forcing dark — a route-scoped `dark` wrapper was rejected because the root layout's inline script sets `colorScheme` on `<html>` from `localStorage`, which a wrapper `div` cannot reach, so a light-mode visitor would have got a light scrollbar and overscroll gutter against a dark page. `/` stays public and is never redirected; the nav CTAs collapse to "Open Dashboard" when `auth()` returns a session. Footer keeps only links that resolve — the mockup's ten placeholder links (Changelog, Docs, Blog, Privacy, Terms…) were dropped rather than shipped as `href="#"`, and the finished page has none. Decorative CSS with no Tailwind equivalent lives in a scoped `.home-*` block in `globals.css` following the `.markdown-preview` precedent, referencing Tailwind's own `--color-*` variables. **A first review pass found three defects, each fixed and re-verified:** `CtaLink` based every variant on `buttonVariants({variant:"outline"})`, leaking that variant's border and background onto the ghost link so the signed-out "Sign In" rendered as an outlined button — invisible in testing until a signed-out render, since every earlier check ran signed-in; `Reveal` created one `IntersectionObserver` per instance where the spec asked for one shared (now a module-level observer with a `WeakMap` of callbacks — instrumenting the constructor shows 14 Reveals sharing 1, the second being Next's own `Link` prefetch observer); and `src/lib/home-content.ts` imported from `src/components/`, inverting the layering, so `CHAOS_SOURCES` moved next to its only consumer. **A design pass using the `impeccable` skill then removed the AI-slop signals the user flagged:** its craft floor refuses gradient text ("emphasis comes from weight or size") and bans an eyebrow above a heading outright, and the hero had both. Gradient fill is gone page-wide (0 gradient-filled text nodes remain) in favour of a size-and-tone step — a quiet 20px lead over a 48–60px payoff, the same device on section headings; the eyebrow pill was deleted; "Let AI do the filing" became "Your knowledge organizes itself" with the Pro chip moved inline; 14 identical fade-ups collapsed to one per section; monospace was reserved for keycaps, code and filenames rather than used as a costume; and `::selection` was themed. A follow-up pass replaced the blue/indigo brand colour with one shared graphite `PRIMARY_SURFACE` across CTAs, the logo mark and the pricing badge, and neutralised the page-wide bluish cast (the ambient wash was three blue/indigo/cyan radials, plus the CTA glow, arrow, preview active row, footer hover, pricing toggle and Pro card) — a scripted sweep of every computed background in the header and main now returns zero blue-dominant fills. Item-type accents were deliberately kept, since they are the app's data language rather than brand colour. **Renamed the user-visible product from DevStash to DevHub** across the homepage, all four auth pages, the sidebar and the Resend verification/reset emails; the `devstash:` `localStorage` keys, the Monaco theme ids and the seeded `demo@devstash.io` login were left alone, as renaming them would silently reset every user's stored theme and sidebar state and break the demo account, the seed, the e2e storage state and several unit tests. The rename orphaned the old "stash" metaphor, so three bits of copy changed ("Your stash organizes itself" → "Your knowledge organizes itself", "Upgrade when your stash outgrows it" → "Upgrade when you outgrow it", "Start Stashing" → "Create Your Account"). Finally, the hero's dashboard preview was made faithful to the real product: the type accent moved from a top rule to `border-l-4` via `getTypeLeftBorderClass` — the exact helper and width `ItemCard` uses — and the sidebar dots took their colours from `getTypeDotClass`, the helper behind the real sidebar's `TypeDot`, so both follow the app automatically. **Two real responsive defects were found by sweeping widths and fixed:** the preview clipped three of its six cards below 640px (fixed height plus `overflow-hidden` while the card grid dropped to one column — now two columns at every width with an adaptive height, 0 clipped cards from 1440 down to 360), and `/dashboard` overflowed horizontally below ~563px because the TopBar's New Item / New Collection buttons could not shrink and the search field's flex parent lacked `min-w-0` (labels now collapse to icons under `sm` and the field gives way; fits to 320px, and the accessible names e2e relies on are unchanged). An earlier fix in the same area: the AI section's code block stretched its grid track past the viewport because grid items default to `min-width: auto`. Tests 303 → 309 (six new for the two added type-colour helpers); no server actions or `lib/db` helpers were added, so per the project's testing scope no further Vitest coverage was warranted. `npm run build`, `npm run lint` (0 errors in `src/`) and the `item-create`, `global-search` and `markdown-editor` Playwright suites (41 specs) all pass. Verification was browser-driven throughout and deliberately discriminating: reduced motion was emulated to confirm the icons are seeded but frozen while an unthrottled run measured drift at ≈26px/s and repulsion at ≈129px/s, matching `DRIFT_SPEED` and `MAX_SPEED`; a patched `requestAnimationFrame` counter froze after a client-side navigation, proving the loop does not leak on unmount. One process note worth remembering: a mid-session e2e run showed all 12 specs failing, which was **not** a regression — Playwright's own dev server from the previous run was still alive on port 3000 and `.next` had been deleted underneath it, the same staleness gotcha this file records for Item Delete and Markdown Editor; every spec passed once the stray servers were killed. Relatedly, `npm run test:e2e` (and `npm run dev`) replace the production build in `.next`, so `npm run start` needs a rebuild afterwards. **Known limitations:** `Reveal` no longer feature-checks `IntersectionObserver` — it is baseline in every supported browser, but the failure mode if that ever changes is that the whole page stays invisible; the prototype in `prototypes/homepage/` is now a superseded duplicate that will drift, and nothing links the two; the chaos field's graph-paper background is an advisory `codex-grid-background` finding from the Impeccable detector, kept because the surface is a literal bounded canvas whose icons bounce off its walls; the six feature cards remain the "same-size cards of icon plus heading plus text" scaffold the craft floor calls a lazy container, kept because the spec pinned six cards; the pricing toggle changes copy but is wired to no checkout, and both plan CTAs point at `/register`; and the pre-existing `src/lib/db/user.test.ts` type error and the `side-tab` detector finding on the markdown blockquote border are untouched. Completed 2026-08-20.
- 2026-08-24: **Auth and Nav Polish** — Added homepage top navigation to sign-in and register pages via AuthLayoutChrome component, matching the existing marketing nav. Dashboard sidebar logo updated to use the same Lucide folder mark as the homepage (Folder icon) and the boxed "ds" initial logo was removed. Homepage nav links (Features, Pricing) work from auth routes using root-relative fragments. Session-aware CTAs on auth page nav (shows "Open Dashboard" when signed in). Completed 2026-08-24.
